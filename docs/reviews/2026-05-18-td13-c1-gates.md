# TD-13 C1-gates — dotnet-architect + security-auditor (BLOCKING)

**Datum:** 2026-05-18
**Batch:** C1 — KMS-envelope krypto-fundament (portar + impl + paket + DI)
**On-disk vid review:** `a039bb0` (ej committat); fixar applicerade ovanpå
**Tester:** 485/485 grönt (480 + 5 nya Major 1-tester)

## dotnet-architect (agentId a6d4107572c68e0e8) — GO

0 Block, 0 Major. Verifierat grönt: Clean Arch/ADR 0009 (port i Application,
impl i Infrastructure, Domain orört, ingen AWS/Npgsql-läcka uppåt), dependency
pinnad+motiverad+same-commit (feedback_di_with_handlers_same_commit ✓),
interface-match mot pre-code-design, valfri-IOptions-ctor-brygga acceptabel,
DI-lifetime (singleton ×3) korrekt, inga §5.1-antipatterns.

3 Minor → åtgärdade in-block:
- **Minor 1:** bred `catch (Exception)` fångade `OperationCanceledException` →
  `catch (OperationCanceledException) { throw; }` tillagd före bred catch i
  båda KmsDataKeyProvider-metoderna.
- **Minor 2:** `ValidateOnStart` saknades trots att ADR 0049 Beslut 4 +
  FieldEncryptionOptions-doc explicit mandaterar det → bytt
  `Configure<>` mot `AddOptions<>().Bind().Validate(CmkKeyId ej tom)
  .ValidateOnStart()`. CC-bedömning: ej CTO-triage — Accepted-ADR har redan
  beslutat (ren conformance, ingen multi-approach; §9.6 in-block-default +
  ADR-mandat entydigt).
- **Minor 3:** kosmetisk regex/IndexOf — se security Minor 3 (åtgärdad).

## security-auditor (agentId ac1913728e8cd33c4) — GO (villkorat, veto EJ utlöst)

| Severity | Antal |
|---|---|
| Critical | 0 |
| High | 0 |
| GDPR | 0 |
| Major | 1 (åtgärdad in-block) |
| Minor | 2 |

Verifierat grönt: krypto-korrekthet (AES-256-GCM, nonce-12 CSPRNG per Encrypt,
tag-16, nonce-unikhet), fail-closed (ingen klartext-fallback, partiell buffert
ZeroMemory på fel-väg), §5.4 nyckelmaterial aldrig i logg/exception
([LoggerMessage] bara owner-Guid+exc-typ), KMS encryption-context identisk
generate/decrypt (kryptografisk cross-user-isolering), EU-routing eu-north-1,
fail-closed vid tom CmkKeyId.

- **Major 1 (obligatorisk, åtgärdad):** DEK-längd (32 byte AES-256) ej
  enforced — `AesGcm` accepterar 16/24-byte tyst → confidentiality-degradering
  utan signal. Fix: `EnsureAes256Dek(dek)`-guard i Encrypt+Decrypt
  (CryptographicException, ingen DEK-byte i message) + 5 nya tester
  (`Encrypt_WithNon256BitDek_Throws` Theory 16/24/31/33 + `Decrypt_WithNon256BitDek_Throws`).
- **Minor 2 (security):** `GeneratedDataKey.PlaintextDek` zeroing-kontrakt
  endast doc-baserat — **noterad för C3-auditen** (interceptor-konsumtion
  måste bevisa zeroing). Ingen C1-ändring (ingen konsument än).
- **Minor 3 (security):** sentinel-versions-allowlist (endast v1) — åtgärdad
  in-block: explicit v1-guard i Decrypt (CryptographicException vid okänd
  version, ADR 0049 Beslut 4 crypto-agility).

## Slutsats

Båda gates GO. Inga Critical/High/GDPR — security-auditor-veto ej utlöst,
ingen Klas-eskalering krävd. Alla Block/Major/relevanta Minor åtgärdade
in-block (§9.6-default) i samma C1-commit. Minor 2 (security) formellt
överlämnad till C3-gaten. Full solution-build grön (0w/0e), 485/485 tester.
