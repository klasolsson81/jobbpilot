# Jobbliggaren — STEG-tracker

> **Version:** 1.29
> **Senast uppdaterad:** 2026-06-13 (**LOGO-ÖVERSYN FAS 1 — Sigillet-marken levererad (ADR 0070).** 4-uddiga kompassen → fyllt civilt registersigill (grön skiva + vit inre ring + 3 liggar-rader, mittenrad guld med bock = loggad post); `BrandMarkSvg` 2→3-fyll via `--jp-mark-*`, `brand-logo` `currentColor`→tokens, wordmark navy→ink, alla brand-ytor (icon/apple/og/twitter satori-verifierade) + manifest theme_color grön. Aktiverar `--jp-gold`; navy-kompass + `#FFCD00`-prick pensionerade → navy-ramp F-städ. ADR 0070 (ny, delvis supersederar ADR 0068 Beslut 1:s logo-not) + DESIGN.md §11/rad 66 + design-skills synkade. Gates: tsc · vitest 868/868 · eslint 0 errors · next build · satori OG/apple gröna. Reviews: code-reviewer ✓ Approved (0/0/3 fixade) + design-reviewer (motion GODKÄND; Major 1 spec-drift fixad; Major 2 spinner-utan-konsument HÅLLEN per Klas → Fas 2 följ-PR). Spinner-impl + header-visual-verify (Playwright-CDN blockerad i container) = Fas 2 / Klas stack. Pågående Fas 5/6-arbete utanför formell tracker — auktoritativ status i `docs/current-work.md`.)
> **Föregående:** 2026-06-13 (**PRODUKTNAMNBYTE JOBBPILOT → JOBBLIGGAREN LEVERERAD (ADR 0069 D1–D7, D3 exkluderad)** — atomisk rename-PR på quiet tree. Steg 0: ADR-PR #62 mergad först. namespace `JobbPilot.*`→`Jobbliggaren.*` (~836 C#-filer, `.sln`, 13 `.csproj`, projektmappar) + DB-namn/Postgres-roller/container/volym + JWT Audience `jobbliggaren-api` + `web/jobbpilot-web`→`web/jobbliggaren-web` + UrlFormat-attribut + `build.yml`/codeql/dependabot-paths + living spec-docs BUILD/CLAUDE/DESIGN (skill-tokens `jobbpilot-*` bevarade) + README + ADR-index. INGEN EF-migration (brand-neutrala DbContexts). **EXKL (D3):** AWS Terraform + `deploy-dev.yml` (separat teardown-PR ADR 0066/TD-104), historiska session-loggar + ADR 0001–0068 + AWS-resurs-intrasslade runbooks/steg-tracker-historikrader/tech-debt + `.claude/`-tooling (medvetet scope-beslut; gamla AWS-identifierare får ej bli falska refs). `.sln` + 13 `.csproj` rekonstruerade från pristine HEAD efter VS Code C# Dev Kit-korruption av projektgrafen. **Test-isolation (rename-collateral, senior-cto-advisor Path A):** `C2ReverseLookupMigrationTests` + `SearchCriteriaJsonbBackcompatTests` rensar `saved_searches` per test (`IAsyncLifetime`) — renamet omsorterade xunit:s namn-ordning och avslöjade latent delad-fixture-koppling; `Api.IntegrationTests` 476/0. Gates: build 0/0, .NET-svit grön, FE build/lint/vitest grön, dotnet format ren. Reviews: code-reviewer 0 Block/1 Major(living-docs→löst)/1 Minor + dotnet-architect ✓ GODKÄND (0/0/0) + security-auditor ✓ PASS (0/0/0). Commit `e6b97ed` + docs-sync. Klas-STOPP: GitHub-repo-rename `jobbpilot`→`jobbliggaren` + logo-PR separat. **Historiska rader nedan behåller "JobbPilot"** — daterade records, korrekt tolkade via ADR 0069. Pågående Fas 5/6-arbete utanför formell tracker — auktoritativ status i `docs/current-work.md`.)
> **Föregående:** 2026-05-24 (**F-Pre Punkt 5b LEVERERAD: gäst-mode-fördjupning** — utbyggnad efter Klas post-deploy-feedback på `v0.2.70-dev` ("för liten", "saknar Jobb-sida", "vill ha modaler precis som live"). Direkt-session efter F-Pre Punkt 5, sekventiellt. **4 commits, alla pushade:** `65b6111` gäst-jobb mock-sida + nav-länk + DEMO-banner (CTO Beslut 2/3/4: ersätt CTA mot mockdata-jobblista paritet med `/jobb`); `08ff285` gäst-modal-paritet via parallel-routes + interception (CTO Beslut 1: paritet med ADR 0053 — `@modal/(.)ansokningar/[id]` + `@modal/(.)jobb/[id]` + fullsidor); `a9b4941` gäst-översikt TodayCard + utbyggd summary + fler notiser (CTO Beslut 5 Variant α — Klas "för liten"-feedback indikerar yta- inte signal-knapphet); `460192c` review-fix-batch (in-block-fix per §9.6: design-M1 status-pill-paritet, M2 source-label, M3 sr-only h1, code-min1 NoticeList-reuse, code-min2 ref-date-konsolidering, sec-m-1). **Tag `v0.2.71-dev` på `460192c`** → deploy-dev-workflow triggad. Reviews (`docs/reviews/2026-05-24-fpre-punkt5b-{cto,code-reviewer,security-auditor,design-reviewer}.md`): design-reviewer NEEDS_REWORK → APPROVED efter 3 Major + 3 Minor in-block; security-auditor APPROVED 0/0/0/0/0/2 Minor; code-reviewer Approved + 2 Minor in-block. Tester vitest 703/703 PASS oförändrat. pnpm build PASS — 4 nya `/gast/*`-routes registrerade (intercepting `@modal/(.)*` + fullsidor `[id]`). Inga TDs lyfta. Inga nya ADRs. Inga BE-ändringar, inga migrations. Pending operativ verifiering: deploy-workflow stable-verify `v0.2.71-dev` + visual-verify `/gast/*` (oversikt-utbyggnad + jobb-mocklista + modal-intercept) + design-reviewer rendered-rond med FAS-DEFERRAL-MANIFEST-prefix post-deploy. Pågående Fas 5/6-arbete utanför formell tracker — auktoritativ status i `docs/current-work.md`.)
> **Föregående:** 2026-05-24 (**F-Pre Punkt 5 LEVERERAD: gäst-mode ("Utforska som gäst") + header-stats label-paritet** — 2 commits denna session: `f02524e` (feature; tag `v0.2.70-dev` på `f02524e` → deploy-dev-workflow triggad) + `6db0c26` (Klas-uppdaterad README synka mot Fas 3-stängning + ADR-count + pre-Fas-4-vertikaler). 28 filer ändrade, +1727/-27 rader. **CTO Variant A:** egen `(guest)/gast/*` route-grupp över B feature-flag / C demo-subdomän — SRP + bounded-context-isolering (Evans 2003), gäst-mode delar inga BE-portar med autentiserad shell. **Klas-STOPP A:** Variant A + Alt 2 (`/gast/jobb` = konverterings-CTA, ingen LIVE i denna leverans — undviker auth-policy-amendment + säkerhetsyta). 18 nya filer (lib/guest/ + (guest)/gast/{layout,4 routes} + components/guest/) + 5 ändrade FE-filer (marketing/page Promise.all-branch, landing-hero-section isAuthenticated-prop, landing-page.test, header-stats label "aktiva"→"aktiva annonser", header-stats.test) + globals.css ~+90 rader (`.jp-demo-banner*` + `.jp-guest-resume*` + `.jp-guest-applist__empty`, token-baserade). Reviews (`docs/reviews/2026-05-24-fpre-punkt5-{cto,code-reviewer,security-auditor,design-reviewer}.md`): alla blockers + majors in-block-fixade (security M-1 httpOnly:true; design B1 aria-current, B2 single primary CTA, M1-M6 inline-styles/hex/magic). Tester vitest 686 → **703 PASS** (+17 nya). pnpm build PASS — 4 nya `/gast/*`-routes. Inga TDs lyfta. Inga nya ADRs. Inga BE-ändringar, inga migrations, inga nya dependencies. **Pending Punkt 5-vertikal:** /jobb LIVE för gäst utestående (kräver framtida session: ADR 0005-amendment + drop `.RequireAuthorization()` på job-ads-GET + JobAdsPublicReadPolicy + security/architect-rondar). Pending operativ verifiering: deploy-workflow stable-verify v0.2.70-dev + visual-verify /gast/* light+dark + landing-CTA-grenar + header-stats-label. Pågående Fas 5/6-arbete utanför formell tracker — auktoritativ status i `docs/current-work.md`.)
> **Föregående:** 2026-05-24 (**F-Pre Steg 5 LEVERERAD: closed-beta-väntelista (Acceptance-modell) + /registrera 308-redirect** — 1 commit `afd8467` (tag `v0.2.69-dev` → deploy-dev-workflow triggad). 30 filer ändrade, +2574/-168 rader. `WaitlistEntry`-aggregatet utökat (Name + Motivation + `AcceptanceSnapshot` VO med MarketingEmailAccepted/AcceptedAt/PrivacyPolicyVersion + RejectedAt). **CTO Fynd 1 Approach B (legal-framing):** ConsentSnapshot → AcceptanceSnapshot per EDPB Guidelines 03/2022 §38–40 + Planet49 C-673/17 — bara MarketingEmail kvar som separat opt-in (Art. 6(1)(a)); villkor + cookies under Art. 6(1)(b) "performance of contract" via disclaimer-text. **CTO Fynd 2 Approach A (PII-erasure):** Reject sentinel-erasar Name/Motivation/Acceptance (GDPR Art. 17), bevarar Email+RejectedAt+AcceptedAt. EF-migration `20260524183703_ExtendWaitlistEntryWithAcceptance` (additiv ALTER TABLE med sentinel-defaults). FE: /vantelista utbyggt formulär + /registrera 308 permanent redirect (RegisterForm.tsx bevarad). Reviews: code-reviewer APPROVED 0 Block/5 Minor; security-auditor APPROVED 1 Major in-block + 2 Minor; design-reviewer NEEDS_REWORK → APPROVED efter 3 Major + 5 Minor in-block-fixade. Tester Domain 422/422 (+18) + Application 591/591 (+3) + Architecture 78/78 + vitest 686/686 (+3). Inga TDs lyfta. Inga nya ADRs (ADR 0005 Amendment 2026-05-12 redan auktoritativ — denna leverans = implementation + CTO-tolkningstillämpning). Pending operativ verifiering: deploy-workflow stable-verify + /vantelista visual-verify + /registrera→308-test. Pågående Fas 5/6-arbete utanför formell tracker — auktoritativ status i `docs/current-work.md`.)
> **Föregående:** 2026-05-24 (F6 P5 Punkt 6 LEVERERAD: page-hero + brand-empty på inre sidor (HANDOVER-v4) — 1 commit `f28236a` (tag `v0.2.68-dev` → Vercel auto-deploy). Ren FE-uppdatering: `.jp-pagehero`-suit + `.jp-empty--brand` införda i `globals.css` med `:focus-visible{outline:2px solid #fff}` per HANDOVER §4.2; applicerade på `/oversikt` (TodayCard i `__aside`), `/ansokningar` (Pipeline-kicker + brand-empty + 2 CTAs), `/cv` (CV-varianter-kicker + omstruktur Tailwind→`.jp-container jp-page`); `/cv` tillagd i `V3_NATIVE_ROUTES` för edge-to-edge. design-reviewer + code-reviewer APPROVED parallellt (0 blockers/0 major, 1 Minor FYI ej lyft per §9.6). vitest 683/683 + .NET 1070 PASS. Inga ADRs, inga BE-ändringar, inga TDs lyfta.)
> **Föregående:** 2026-05-24 (STEG 6 MVP-recall-fix FULLT LEVERERAD — recall "systemutvecklare" 162 → 1133 hits (×7, 141% över 600-mål) via Approach A SSYK backfill + Approach B SSYK-expansion + Plan B Terraform Hangfire-secret-fix. Tags `v0.2.66-dev` + `v0.2.67-dev`. TD-96/97/98/99 lyfta.)
> **Föregående:** 2026-05-24 (F6 P5 Punkt 3-svans LEVERERAD: inloggad-stats + Worker→Redis-incident-fix + freshness-tagg-sync + Terraform-image-tag-fix — 4 commits ovanpå `v0.2.60-dev`: `e0911a3` HeaderStats i app-header (10-min-polling + delta-affordans, vitest +8); `9d53bb4` Worker→Redis-anslutning + **ADR 0023 Amendment 2026-05-24** (Worker får legitim outgoing Redis-write-port via ADR 0064, HTTP-fri-invariantens kärna intakt; dotnet-architect-skriven verbatim agentId `a9446dac40e8fef02`); `79e063f` freshness-tagg UTC-kalender-dag-sync; `69acdd5` terraform variable-validation + **TD-91** RDS-drift (paritet TD-85). Tag `v0.2.61-dev` på `69acdd5`, deploy-dev run `26352086921` triggad ~04:40 UTC. Live verifierat `{"activeCount":46328,"newToday":7,"isStale":false}`.)
> **Föregående:** 2026-05-24 (F6 P5 Punkt 3 Landing live-stats FULLT LEVERERAD & DEPLOY-TRIGGAD — `GET /api/v1/landing/stats` anonym via Worker-precomputed Redis-cache, Hangfire-cron `*/5`, CTO Variant B vald över cache-aside/PG materialized view/Next.js revalidate, ADR 0064 NY Accepted + ADR 0056 Amendment, NBomber-scenario etablerat, tag `v0.2.60-dev` på `13d172d`, deploy-run `26350409884` queued, alla reviewer-gates APPROVED, TD-89 self-vetoad.)
> **Föregående:** 2026-05-19 (TD-13 FAS 3.5 STÄNGD — den enda genuina Fas 2-TD:n (fotnot ⁶) levererad som pre-FAS-4-blocker: per-användar-DEK KMS-envelope (ADR 0049) på 4 PII-kolumner + backfill + crypto-erasure, `v0.2.19-dev`-deploy grön på dev, security-auditor + code-reviewer GO, svit grön, TD-13 arkiverad §9.7. **FAS 4 avblockerad** (kräver egen Klas-GO + ren /clear). Öppet ej-blockerande: ADR 0049 Not 6-reconciliation Klas-granskning, TD-85 IaC-drift, Beslut 5 cutover/drop framtida Klas-STOPP.)
> **Föregående:** 2026-05-17 (**FAS 2 FORMELLT STÄNGD.** Samlad session Batch 0–6: ingestion-hybrid-fix (ADR 0032-amendment, storm-borta CONFIRMED) + sök-yta-omdesign B–E (ADR 0042 + ADR 0039 Beslut 3-supersession). Deployad `v0.2.10-dev` (2 migrations) + Vercel. 7 Klas-STOPP, alla reviewer-gates, design-reviewer APPROVED. Klas input-regel 2026-05-17 (rena inputs) + design-skill-kodifiering. Fas 2-TD-triage: TD-13/27 Fas 2-defer Klas-bekräftad (EDPB-omverifierad), övriga "Fas 2"-TD = Trigger/skala (ej genuin skuld). Fas 2-raden → **Klar 2026-05-17**, fotnot ⁶ tillagd. Fas 3 kräver explicit Klas-GO för sessionsbyte.)
> **Föregående uppdatering:** 1.20 / 2026-05-12 ~08:00 (Fas 1-rensning komplett — batches B–F, 16 TDs stängda, Fas 1 Minor-sektion TOM)
> **Roll:** permanent översikt över STEG- och fas-progression.

Kompletteras av:
- `docs/current-work.md` — aktiv session-state
- `docs/sessions/` — per-session-loggar
- `docs/decisions/` — arkitekturbeslut
- `docs/tech-debt.md` — teknisk skuld

---

## 1. Översikt

JobbPilot:s utvecklingsbana spårar två dimensioner:

- **Faser** — strategiska tidsblock per BUILD.md §18. Nio faser plus Fas 9+ (efter klass-launch).
- **STEG** — tekniska arbetsenheter inom faser. Numrering är historisk och behålls oförändrad även när faserna inte mappar 1:1 mot STEG-gränserna.

Mellan-arbete (upptakter, cleanup-passningar, disciplin-uppgraderingar) är inte STEG men dokumenteras i §4.

## 2. Fas-översikt (per BUILD.md §18)

| Fas | Namn | Tidsuppskattning | Milstolpe | Status |
|-----|------|------------------|-----------|--------|
| Fas 0 | Foundation | ~2 v | Registrera + logga in på dev.jobbpilot.se | **Klar 2026-05-10** ¹ |
| Fas 1 | Core Domain | ~3 v | CV manuellt + "fake" ansökningar i admin-audit | **Klar 2026-05-11** ³ + **rensad 2026-05-12** ⁴ |
| Fas 2 | JobTech Integration | ~2 v | Söka jobb på Platsbanken via appen, spara sökningar | **Klar 2026-05-17** ²⁵⁶ |
| Fas 3 | Application Management | ~2 v | Fullständig ansökningshantering (utan AI) | **Klar 2026-05-18** ⁷ |
| Fas 4 | AI Layer | ~3-4 v | Alla AI-features end-to-end + 14 dagar dogfood | Planerad ⁸ |
| Fas 5 | Integrationer | ~2 v | Gmail auto-loggar, intervjuer i Google Calendar | Planerad |
| Fas 6 | Admin & Analytics | ~2 v | Admin-panel komplett | Planerad |
| Fas 7 | Internal Beta | ~2 v | 3 användare aktivt 14 dagar | Planerad |
| Fas 8 | Klass-launch | ~1 v | 20 klasskamrater onboardade — v1 klar | Planerad |
| Fas 9+ | Efter klass-launch | — | Mobil, Kanban, intervjuträning, LinkedIn, Stripe, Chrome ext m.m. | Framtid |

**Totalt:** ~20 veckor till klass-launch (mjuk uppskattning, inga hårda deadlines).

⁸ Fas 4 fortsatt **Planerad** (ej statusflytt). Pre-Fas-4-grind tillkom
2026-05-19 (post-Fas-3 migration-discovery-session): **ADR 0051** (Proposed) —
Bedrock utgår, Anthropic Direct API (systemnyckel + BYOK), US opt-in även
systemnyckel; **5 icke-förhandlingsbara GDPR-villkor** (DPIA Art. 35 / SCC+
Schrems II-TIA+Anthropic-DPA+DPF / versionerad privacy-policy före live /
Art. 25-opt-in / ADR 0049-decrypt-interaktion) måste vara gröna **innan första
Fas-4-AI-kodrad**. Relaterat: **ADR 0050** (Proposed) full AWS-exit → Hetzner
CX32; **KMS-migrations-blocker** (ADR 0049 PII-krypto på AWS KMS måste
om-hemmas) löses i egen designomgång före faktisk migration. Spåras här +
`docs/current-work.md`, EJ `tech-debt.md` (greenfield-roadmap, ej debt — §9.6).

¹ Fas 0 fullt klar 2026-05-10 efter STEG 14c. Alla milstolpar uppfyllda: Clean Arch-solution + Identity + Next.js + design system + AWS-infrastruktur (VPC/RDS/Redis/ALB/ECS/ECR) + Container-pipeline + DNS + TLS (HTTPS aktiverat med HSTS) + GitHub OIDC-baserad CI/CD + first formal tag-deploy `v0.1.0-dev` end-to-end PASS + Bootstrap-IAM-user raderad. Live på `https://dev.jobbpilot.se/api/ready`.

² Fas 2 är blockerad till ADR 0005 (go-to-market) är beslutad och kostnadsskydd implementerat (Budget Actions, `registrations_open`-flagga, rate limiting, runbook `docs/runbooks/aws-cost-recovery.md`) per BUILD.md §18. **Alla prereqs avklarade 2026-05-12** (F2-P0 invitations + F2-P1 feature-flag + F2-P2 rate-limits + F2-P3 Budget Actions + F2-P4 cost-recovery-runbook + F2-P6 readiness-probe-split / TD-29 stängd). JobTech-features (P7 paginering + P8 integration) får startas.

³ Fas 1 fullt klar 2026-05-11. Milestonestängning levererad i stationär-CC-session: admin-roll-infrastruktur (per-request claims A1 + IdempotentAdminRoleSeeder B1) + GET /api/v1/admin/audit-log + frontend `/admin/granskning` med Server Components + zero client-JS för core-flöde. 5 parallella agent-reviews APPROVED (code-reviewer + security-auditor + dotnet-architect + design-reviewer × 2 frontiers). CTO-triage med 12 in-block-fixar applicerade per 4-timmarsregeln + 6 nya TDs (TD-50 till TD-55) + ADR 0028 (admin-authorization defense-in-depth, marker-interface + HTTP-policy dubbel-gate).

⁴ Fas 1-rensning komplett 2026-05-12 (lång CC-session 2026-05-11 21:00 → 2026-05-12 08:00). Levererade Fas 1-rensningens batches B–F: TD-1 + TD-2 (a11y), TD-3 + TD-4 + TD-5 (UX-pass /mig), TD-6 + TD-28 (me-flöde fullstack med ny `/auth/verify`-endpoint per Klas-Alt1), TD-12 (cross-user-isolation, 7 tester), TD-40 (retroaktivt), TD-41 + TD-57 (shadcn-first form-controls). Disciplinretur: TD-65 (Playwright E2E) + TD-66 (Resume/JobSeeker cross-user-isolation) stängda efter Klas-feedback om TD-lyftnings-disciplin. Plus TD-25 (HardDeleteAccountsJob resilient loop), TD-67 (failed-access-detection via IFailedAccessLogger + ADR 0031), TD-68 (CloudWatch security-alarms — Terraform-modul + dev-apply genomförd, jobbpilot-dev-secops-anomaly SNS-topic + 2 alarms live). 16 TDs stängda. Fas 1 Minor-sektionen i tech-debt.md är TOM. Lärdom sparad i memory: TD-lyftningar måste pressas mot §9.6-kriterier — "scope-disciplin per batch" eller "+1-2h CC-tid" är inte legitima skäl.

⁶ **Fas 2 FORMELLT STÄNGD 2026-05-17.** Samlad session (Batch 0–6): ingestion payload-trunkerings-rotorsak verifierad (Batch 0 CloudWatch) → hybrid-fix (ADR 0032-amendment Accepted; snapshot trunkerings-tålig enumeration-boundary-catch + bounded retry, stream oförändrat; storm-borta CONFIRMED på dev efter `v0.2.9-dev`; konvergens-risk medvetet accepterad, korpus-tillväxt-trajektoria = gate-def per Klas) + sök-yta-omdesign B–E (ADR 0042 Accepted + ADR 0039 Beslut 3 partiell supersession): B `SearchCriteria` Ssyk/Region single→multi (CTO Yta A3 HasConversion-converter), C typeahead C1 (lokal `job_ads.Title` ILIKE-prefix + btree functional partial-index), D relevans-sort D2-ILIKE, E "Ny"-badge (`Since`/`IsNew`), A kollaps-filteryta + multi-select + live-typeahead frontend. Deployad `v0.2.10-dev` (Phase E 2 migrations applied: `F2SearchCriteriaMultiValue` + `F2SuggestTitlePrefixIndex`) + Vercel. 7 Klas-STOPP passerade; CTO×7/architect×3/security-auditor×3 PASS/code-reviewer×6 GO/db-migration-writer×3/test-writer/adr-keeper/design-reviewer **APPROVED (VETO lyft run 0147)**. Klas hård input-regel 2026-05-17 (rena input-fält, ingen exempel-placeholder, hint via aria-describedby) tillämpad + kodifierad i jobbpilot-design-components/-copy-skills; ADR 0038 placeholder-formulering upphävd. Svit 1083 backend + 357 frontend grön, build 0/0. Klas verifierar rena auth-fält live (fresh auth-korpus blockerad av Vercel Attack Challenge Mode — infra, ej kod; design-reviewer källgranskade input-regeln verbatim). **Fas 2-TD-triage (Klas-direktiv "gör Fas 2-TD före stängning"):** senior-cto-advisor-dom — TD-13 (Encryption PII-kolumner, enda genuina Fas 2-TD) + TD-27 (dependency på TD-13) Fas 2-defer **håller** (EDPB CEF 2025 web-omverifierad 2026-05-17: RDS KMS at-rest = Art. 32-standardpraxis, app-side envelope = defense-in-depth utöver krav, crypto-erasure ej Art. 17-krav; Klas-bekräftad 2026-05-17 så framtida CTO ej återupprepar); övriga "Fas 2"-TD (TD-19/23/24/62/63/74/82) = Trigger/skala/opportunistisk/saknad-dependency, ej genuin Fas 2-skuld (etikett-städning = separat docs-keeper-touch). Inga genuina Fas 2-skulder oadresserade → Fas 3 (Application Management) får startas (kräver explicit Klas-GO för sessionsbyte per §9.2).

⁵ Fas 2-milstolpen "söka jobb på Platsbanken + spara sökningar" **funktionellt nådd 2026-05-16**. JobTech-ingestion kod-komplett + deployad (`v0.2.6-dev`), pending cron-live-verifiering (separat lokal session, AWS SSO). "Spara sökningar" levererad end-to-end (ADR 0039 Accepted): `SavedSearch` AR + `SearchCriteria` VO + 6 endpoints JobSeeker-scoped + `JobAdSearch` delad SPOT-modul + frontend Spara/lista/kör/radera. Commits `b82e7cf`→`d602968`. ADR 0040 (Proposed) — framtida smart CV-baserat filter (Fas 4+), Klas-grundtanke CTO-vägd. TD-84 lyft (mutationsendpoints NotFound→400, projekt-brett, Minor/Trigger). **Uppdaterat 2026-05-16:** deployad `v0.2.7-dev`+`v0.2.8-dev` (migration `F2SavedSearches` applicerad, EventId 63). **Auth-gated visuell verifiering KLAR** (visual-verify.ts auth-läge, CTO Variant A; dedikerat dev-test-konto, cred Variant C utanför repot) — design-reviewer 0/0/0 efter ADR 0041 a11y-fix (WCAG 1.4.11 dark-dialog-Blocker i delad `ui/dialog.tsx` → token `--jp-border-modal`, live-verifierad RESOLVED). Rök-test live grönt (create/list/run-137/scoping-404/delete). security-auditor PASS. Commits `12fc9e6`+`64a6bf8`. **Kvar innan Fas 2 formellt stängs: ENDAST ingestion-cron-verifiering** (separat lokal session, AWS SSO, EventId 5402 + `job_ads`-korpus) + ev. Fas 2-städning. (b) visuell + (c) rök = klara. ADR 0005-observation: dev-test-konto via icke-flag-gejtat /register, triageras i auth-touch.

⁷ **Fas 3 FORMELLT STÄNGD 2026-05-18.** Discovery bröt FAS 3-startpromptens greenfield-premiss: hela Application-pipeline-vertikalen (Domain 10-state-machine + FollowUp/Note + 6 events, 5 commands/3 queries/DTOs, DetectGhostedJob, EF+2 migrations, 7 endpoints, Worker recurring, 3 frontend-rutter, 12+ testfiler) byggdes redan i Fas 1. senior-cto-advisor `a49fdd7992b3a7a0a` + Klas-beslut: redefinierad FAS 3 = A (RecordFollowUpOutcome-vertikal) + D (DoD-verifiering befintlig 95%-vertikal); B Påminnelser→Fas 5; C Avslags-analys→Fas 6 (BUILD.md §18 — medveten dokumenterad avvikelse). **Batch 1** (commit `78d3b14`, CI-grön run `25998180368`): D-verifiering (svit 1160/1160, ADR 0044-golv alla PASS) + A RecordFollowUpOutcome (domän+app+api+frontend, latent Fas 1 enum-bugg rättad). Klas underkände dock `/ansokningar` live 2 ggr (UUID-rader, ingen visuell hierarki, fel status-mönster) → strukturerad STOPP-kedja: STOPP 1 discovery → STOPP 2 plan (`docs/design/ansokningar-redesign-plan.md`) → STOPP A skrivväg + **ADR 0048 Accepted** (cross-aggregat-read-join) → **STOPP 3a backend** (commit `46291c0`: `ManualPosting` VO + invariant `JobAdId ⊕ ManualPosting` + migration `20260517222003_AddManualPostingToApplications` + 3 read-handlers LEFT JOIN, deployad `v0.2.15-dev`) → **STOPP 3b frontend** (commit `47a1378`: `ApplicationRow`, `/ansokningar/[id]`-omarbetning, ny Radix `radio-group`, JobInfoPanel, StatusEditCard, manuell skrivväg `/ansokningar/ny`; deployad `v0.2.16-dev` run `26014066232` success). **Stängnings-session 2026-05-18 (laptop-handoff):** visual-verify auth-läge utökad till 5 jobbidentitets-tillstånd + L2-destruktiv-Dialog-capture (commits `8530d9b`+`38425be`); design-reviewer bindande Area 5 render-VETO (ADR 0047) v1 1 Block/1 Major/1 Minor → tooling-re-work (senior-cto-advisor 2-ronds-triage: Block1 falsk-pos, Major1 = bekräftad Chromium/CDP-emulerings-instrumentartefakt med dispositivt produktkod-invariansbevis, ej produktdefekt) → **v2 PASS 0/0/1** (Minor = m3-uppskjuten datetime-local, ej blocker; commit `22338ea`). **Klas live-verify /ansokningar = GODKÄND 2026-05-18** (bindande grind efter 2 underkännanden; dark-toggle i Klas egen browser bekräftade dark fungerar = auktoritativ kompensation för instrument-artefakten). **ADR 0046 Proposed→Accepted 2026-05-18** (Grind 1, explicit Klas-GO, adr-keeper). **Defer-not:** `jobad-kopplad`-dark visual-verify-snapshot = känd instrument-artefakt (CDP colorScheme + extern-target-popup-kontext), exkluderad från snapshot-gaten tills Playwright/Chromium-uppgradering; Klas browser-toggle = auktoritativ dark-bekräftelse. Inga nya TDs (§9.6 — allt in-block/in-fas). **Post-stängnings-backlog (Klas-vald sekvensering "separat efter stängning", egen plan-design):** /ansokningar list-skannbarhet (status-snabblänkar + minimera/maximera-grupper, designas tillsammans) + dark-kantlinje-token-kontrast (ADR 0041-amendment/ny ADR — `--jp-border` #1E293B ≈ dark-surface, samma klass som ADR 0041 men ej åtgärdad utanför modaler). Se `docs/sessions/2026-05-18-*-fas3-stangning.md`.

## 3. STEG-historik

STEG-numrering följer faktisk arbetsutveckling och mappar inte exakt mot fas-gränserna i BUILD.md §18. Se §7 för numreringsfotnot.

### Klara

| STEG | Fas | Beskrivning | Sessions |
|------|-----|-------------|----------|
| Pre-STEG | Fas 0 | Research, plan, design-research | Sessions 1, 2, 2.5 |
| Pre-STEG | Fas 0 | AWS, Docker, agents, skills, hooks, GitHub, docs | Sessions 3-5 |
| STEG 1 | Fas 0 | .NET Solution-uppsättning + ADR 0008-0011 | Session 6 |
| STEG 2 | Fas 0 | Domain/Infrastructure/Application/API — JobAd aggregate (35 tester) | Session 7 |
| STEG 3 | Fas 1 | Auth-stack (Identity + JWT, ADR 0012-0014) + JobSeeker aggregate (75 tester) | Session 8 |
| STEG 4a | Fas 0 | Frontend bootstrap (Next.js 16, civic-tokens, shadcn nova, ADR 0015-0016) | Session 9 |
| STEG 4b | Fas 1 | Session-auth backend (ISessionStore, SessionAuthenticationHandler, IAuthAuditLogger, ADR 0017-0018) + frontend auth (login/register/me-sidor, /(app)-layout, 153 tester) | Session 4b.1, 4b.2 |
| STEG 5 | Fas 1 | Application-aggregat — domän (SmartEnum state machine, FollowUp, ApplicationNote), EF Core, 5 commands, 3 queries, 7 API-endpoints, 280 tester (53 nya) | 2026-05-07 |
| STEG 6 | Fas 1 | Frontend /ansokningar — pipeline-tabell, ny-ansökan, detaljvy, transitionsformulär, Server Actions, Zod v4, 28 Vitest + 13 Playwright E2E | 2026-05-08 |
| STEG 7a | Fas 1 | Resume-aggregat backend — domain (Resume AR + ResumeVersion + ResumeContent VO), EF JSONB via HasConversion, migration `AddResumeAggregate`, 5 commands + 2 queries, 7 API-endpoints, +98 tester. Plan-design via CC (utan webb-Claude). ADR 0021 (Master-mutation), TD-13/TD-14. | 2026-05-08 |
| STEG 7b | Fas 1 | Frontend /cv — Resume-pages, ResumeContentForm med RHF `useFieldArray` för Experiences/Educations/Skills, Server Actions, Zod v4, 37 Vitest + 6 Playwright E2E. TD-15. | 2026-05-08 |
| STEG 8 | Fas 1 | Audit log-infrastruktur — pipeline-behavior + marker-interface (ADR 0022). 10 commands märkta IAuditableCommand. Migration `AddAuditLogTable`. IP-anonymisering /24+/48, server-gen correlation-ID. Stänger TD-9. +41 tester (14 Domain + 11 Application + 12 Integration + 4 Architecture). | 2026-05-08 |
| STEG 9 | Fas 1+2/3 förskott | Worker-pipeline-aktivering + Hangfire-infrastruktur (ADR 0023). DI-modulär refaktor (`AddPersistence`/`AddIdentityAndSessions`/`AddHttpAuditing`). 3 Worker-stubs av audit-portarna. `DetectGhostedApplicationsJob` orchestrator + `StaleApplicationSpecification`. Application-aggregat utökat med `LastStatusChangeAt` + `GhostedThresholdDays` (per Application, BUILD.md §schema). Migration `AddApplicationStaleDetectionFields` (NOW()-backfill, partial index). **Pipeline-bug-fix:** `AddMediatorPipelineBehaviors()` (open-generic DI) ersätter trasig `options.PipelineBehaviors`-fält-reference. Newtonsoft.Json 13.0.3 transitiv CVE-pinning. +32 tester (9 Domain + 12 Application + 5 Architecture + 6 Worker SmokeTest). | 2026-05-08 |
| STEG 10a | Fas 1 | Audit-log retention via PostgreSQL native daily partitioning + Hangfire-jobb (ADR 0024 D1+D2). `audit_log` konverterad till `PARTITION BY RANGE (occurred_at)` med komposit-PK `(id, occurred_at)`. Migration `AddAuditLogPartitioning` (rename → 7 bootstrap-partitions + default → INSERT-SELECT med explicit kolumnlista → DROP legacy). `IAuditPartitionMaintainer`-port + impl + `AuditLogRetentionJob`-orchestrator. Hangfire-cron 03:00 UTC daily. Idempotent (`CREATE IF NOT EXISTS`). 3 nya arch-tester för bypass-isolering. 4 nya smoke-tester. Runbook `docs/runbooks/audit-retention.md`. **Stänger del 1 av TD-16** (Art. 5(1)(e) Storage Limitation). TD-20 ny (defensiv refactor av SqlQueryRaw → SqlQuery<FormattableString>, defererad). +7 tester (3 arch + 4 smoke). | 2026-05-08 |
| STEG 10b | Fas 1 | DELETE /me + GDPR Art. 17-cascade + HardDeleteAccountsJob (ADR 0024 D3+D4+D5+D6). `IAuditTrailEraser`-port + impl (audit-bypass via direct SQL UPDATE). `DeleteAccountCommand` + handler (cascade soft-delete + idempotent). `DELETE /api/v1/me`-endpoint + post-commit session-invalidering via secondary Redis-set. `LoginCommandHandler` D5-blockering vid `JobSeeker.DeletedAt` (returnerar `Auth.InvalidCredentials`, inte `AccountPendingDeletion` — sec-fix mot info disclosure). `IAccountHardDeleter` + `AccountHardDeleter` cross-context impl. `HardDeleteAccountsJob` orchestrator: Steg 0 orphan-cleanup + Steg 1 hämta mogna + Steg 2 explicit transaction (anonymize audit + hard-delete cascade) + separat Identity-DELETE-boundary. Cron 04:00 UTC daily. `AddCoreIdentityForWorker`-extension (HTTP-fri Identity för Worker, utan AddDefaultTokenProviders). 4 nya arch-tester + 6 nya smoke-tester + 5 nya integration-tester + 2 unit-tester (D5). Runbook `docs/runbooks/account-deletion.md`. **Stänger TD-16 helt.** TD-21-25 nya (rate-limiting prod-blockare, app-logg-retention prod-blockare, Redis MULTI/EXEC defensiv refactor, cascade-paginering Fas 4, per-konto try/catch opportunistiskt). +17 tester (2 unit + 4 arch + 6 worker smoke + 5 api integration). | 2026-05-08 |
| STEG 11 | Fas 1 | **Fas 1 prod-deploy-blockare-cleanup** (TD-22 → TD-17 → TD-21). **TD-22** stängd: `IIpAnonymizer`-port (Application) + impl (Infrastructure) lyft från `RequestContextProvider`-private metod, konsumeras nu även av `AuthAuditLogger` så app-loggens IP /24+/48-anonymiseras vid logg-tid. ADR 0024 D7 (App-logg-redaction + 30d CloudWatch-retention som matchar Art. 17-fönstret). EmailHash-HMAC defererad till Fas 2 → ny TD-27. **TD-17** stängd 5/6 punkter: `HangfireWorkerOptions` config-driven (allow-list-defense `IsDevelopment\|\|IsEnvironment(Test)` för PrepareSchemaIfNecessary, fail-loud-throw utanför), `BackgroundJobServerOptions.ShutdownTimeout=25s` + `HostOptions.ShutdownTimeout+3s` (Fargate SIGTERM 30s grace), cron-kollision åtgärdad (detect-ghosted 03:00 → 03:30 UTC), runbook `hangfire-schema.md` (Install.sql-export, GRANT-modell med REVOKE PUBLIC, 8-punkts dashboard-auth-checklista, kalibrerings-fas, idempotency-tabell). Punkt 4 (ConnectionStrings split) defererad till Fas 0-stängning (kräver två AWS Secrets Manager-poster). **TD-21** stängd: tre rate-limit-policies (account-deletion 1/60s per UserId med NoLimiter för anonymous, auth-write 20/min per IP, auth-loose 30/min per IP), `UseForwardedHeaders` middleware (KnownNetworks-prod-konfig defererad till runbook §3.3 pre-launch-gate), `OnRejected`-callback med LoggerMessage source-gen + Retry-After-header (RFC 6585), separat `StrictRateLimitApiFactory` för isolerad 429-integration-test, `parallelizeTestCollections=false` så env-var-overlay inte race:ar. Frontend typed-confirmation-UX defererad → ny TD-28. **Reviews:** alla 3 backend-block fick security-auditor + code-reviewer parallellt (TD-21 även re-review efter Sec-Major-fixes). 0 Critical/Blocker, alla Major-fynd fixade in-block. +27 tester (8 IpAnonymizer + 4 AuthAuditLogger inkl IPv6 + 1 IIpAnonymizer arch-test + 5 HangfireWorkerOptions + 6 RateLimitingOptions + 2 AuthWriteRateLimit + 1 strict-fixture). Backend totalt 502 tester gröna. | 2026-05-09 |

### Klara (forts.)

| STEG | Fas | Beskrivning | Sessions |
|------|-----|-------------|----------|
| STEG 12 | Fas 0 | **Kod-pre-launch-gates inför första prod-deploy** (Alt A1 av A4-sekvens A1→A2→A3). **Block 1:** TD-17 punkt 4 stängd — `HangfireConnectionStringResolver` lyft till statisk testbar metod, fallback-kedja `HangfireStorage → Postgres` så prod kan splitta access-yta (jobbpilot_worker DML-only) utan dev-overhead. Worker `appsettings.Production.json` overlay (PrepareSchemaIfNecessary=false + ShutdownTimeoutSeconds=25). **Block 2:** TD-21 KnownNetworks stängd — `ForwardedHeadersConfig` config-driven med fail-loud parse (System.Net.IPNetwork + KnownIPNetworks per .NET 10). Production-defense `EnsureSafeForEnvironment` allow-list `IsDevelopment\|\|IsEnvironment("Test")` (Sec-Major-1 fixad in-block) — tom KnownNetworks utanför dev/test → uppstart-throw → ECS-container startar inte. Api `appsettings.Production.json` overlay (KnownNetworks tom som pre-launch-gate, ForwardLimit=1 för ALB-only). **Block 3:** Två overlay-filer + comment-stilad i sektioner. **Sec-Major-2 docs-fix:** `aws-setup.md §3.3` förtydligad om CloudFront edge-IPs i AWS-managed prefix-list (com.amazonaws.global.cloudfront.origin-facing) — bara VPC-CIDR räcker inte för ForwardLimit=2; ALB-only-deploy ska sätta 1. **Reviews:** security-auditor (Sec-Major-1+2 fixade in-block, 3 Minor + 2 Nit defererade) + code-reviewer (0 Major, M1-M3 fixade in-block, 4 Nit "rätt val"). +35 tester (5 HangfireConnectionStringResolver + 17 ForwardedHeadersConfig parse + 14 EnsureSafeForEnvironment + 1 strict-fixture artifact). Backend 537 totalt (157 Domain + 183 Application + 23 Architecture + 26 Worker + 148 Api Integration). | 2026-05-09 |
| STEG 13a | Fas 0 | **APPLIED 2026-05-10.** **Infra-as-code-stack: networking + databas + cache** (Alt A2 första block). Tre nya Terraform-modules + ny `environments/dev/` env-stack: **`modules/network/`** — VPC `10.0.0.0/16` 3-AZ, public/private subnets, single NAT (cost-optimized AZ-a), VPC Endpoints (S3 Gateway + Secrets Manager + KMS Interface; Bedrock utelämnad pga region-mismatch eu-north-1 ↔ eu-central-1), 5 strikta SGs (alb/ecs/rds/redis/vpce). **`modules/rds/`** — Postgres 18.3 db.t4g.medium Multi-AZ, KMS-encrypted storage + master-secret + Performance Insights, AWS-managed master-pwd via Secrets Manager (auto-rotation 7d), Enhanced Monitoring 60s, deletion_protection. **`modules/redis/`** — Valkey 8.0 replication group cache.t4g.small × 2 noder, multi-AZ failover, transit + at-rest encryption, AUTH-token (64 chars `[a-zA-Z0-9]`, ~380 bits entropy) i Secrets Manager. **`environments/dev/`** — komponerar modulerna via `data "aws_kms_alias"`-lookup mot baseline-master-key, två dev-secrets-placeholders (db/app + db/hangfire-storage) som sätts post-DDL i STEG 14. **Sec-Major-1 + Sec-Minor-6 fixade in-block** — RDS parameter-group: `log_statement=none` (hindrar password-leak vid STEG 14 DDL), `log_parameter_max_length=0` + `_on_error=0` (trunkerar bind-värden i slow-query-log → ingen PII via WHERE), explicit `aws_cloudwatch_log_group` med 30d retention + KMS (uppfyller ADR 0024 D7). **Sec-Major-2 ADR-accepterad** via ADR 0025 (ECS-egress 0.0.0.0/0 Fas 0-acceptance, omvärderingstrigger Fas 1→2 + förberedd hardening-väg). 5 Minor + 3 Nit defererade. **Ingen .NET-kod rörd** (537 tester oförändrade). **Inte applied** — kräver SSO-login, budget-höjning ($50→$200), version-verifiering (Valkey 8.0, postgres18 family-sträng) som operativa pre-apply-steg. | 2026-05-09 |
| STEG 14b | Fas 0 | **APPLIED 2026-05-10** (Migrate-task exit 0 efter 5 iterationer, Worker recovery COMPLETE). **DDL-init + Worker-recovery via one-shot ECS-task** (2 av 3 14-block per Klas-uppdelning). Klas-val: Väg B (one-shot ECS-task med PostgreSqlObjectsInstaller) över Väg A (psql + Session Manager). Ny `src/JobbPilot.Migrate/` console-app — 4 phases (master-DDL → Hangfire-Install → master-GRANTs → Secrets PutSecretValue), idempotent, CancellationToken-flow. Ny `task_migrate`-roll i modules/iam_ecs (count-pattern, separat blast-radius). Migrate-task-def i modules/ecs + `migrate_run_task_command` Terraform output. ECR-repo + KMS-encrypted LogGroup för migrate. **3 agent-reviews:** security (APPROVE-WITH-FIXES, 2 Sec-Major + 5 Sec-Minor — Sec-Minor-5 ta-bort-GenerateDataKey **empiriskt motbevisad** vid Phase D), code-reviewer (APPROVE-WITH-FIXES, 2 Major + 6 Minor + 3 Nit), dotnet-architect (APPROVE-WITH-FIXES, 2 Viktigt + 3 Mindre — Task.Run-wrap förkastad enligt CLAUDE.md §3.5). **5 debug-iterationer** för run-task: (1) DO-block + Npgsql-@param fungerar inte i pl/pgsql-scope → två-stegs SELECT+DDL-pattern, (2) `42501: must be able to SET ROLE` (RDS-master limited superuser) → `GRANT migrations TO CURRENT_USER`, (3) AWS CLI charmap-error på `→`-tecken i Windows-shell → byt till `->` ASCII, (4) `Access to KMS is not allowed` på PutSecretValue → återställ kms:GenerateDataKey i task_migrate-policy, (5) **exit 0** — alla 4 phases PASS. Apply-state: 3 Postgres-roller skapade, hangfire-schema + 13 tabeller, GRANT-modell aktiv (worker = DML-only på hangfire.*), connection-strings skrivna till Secrets Manager. Worker + API force-new-deployment → båda COMPLETED stable inom ~3 min. Smoke-test https://dev.jobbpilot.se/api/ready → 200 + HSTS aktiv. Hangfire BackgroundJobServer running med 4 workers + 7 dispatchers (ServerWatchdog, JobCancellationWatcher, ExpirationManager, CountersAggregator, Worker, DelayedJobScheduler, RecurringJobScheduler). TD-37 deferrad till 14c (Integration-tests CI-fail kräver Linux-Docker repro). TD-38 ny (Trust Server Certificate=true persisterad i app/worker connection-strings — Fas 1 hardening). Spend ~$79.65/mån (+$0.15). | 2026-05-10 |
| STEG 14a | Fas 0 | **APPLIED 2026-05-10** (OIDC-resurser i AWS + workflows pushade, build.yml mekaniskt verifierad). **GitHub OIDC + CI/CD-pipeline** (1 av 3 14-block per Klas-uppdelning). Ny modul `modules/github_oidc/` (versions/variables/main/outputs) — OIDC-provider + dev-deploy-roll scope:ad strikt till `refs/tags/v*-dev` (M1+M2 strict från security-auditor). Inline-policy least-privilege: ECR push (api+worker), ECS UpdateService, `iam:PassRole` med `iam:PassedToService=ecs-tasks.amazonaws.com`-condition + roll-allow-list (gold-standard mot privilege-escalation), RegisterTaskDefinition `*`-bound (AWS-API-begränsning, mitigeras via PassRole), Logs read-only för deploy-debug. `max_session_duration=3600`, `role-duration-seconds=900` (kort blast-radius). `thumbprint_list=[]` (AWS Trust Store sedan 2023). Anropas från prod/baseline (delad resurs, KMS/Route53-pattern). Outputs: `github_oidc_provider_arn` (delas av framtida staging/prod-roller), `github_actions_deploy_dev_role_arn` (satt som GH secret AWS_DEPLOY_ROLE_ARN). **Workflows:** `build.yml` (push main + PR + workflow_dispatch) — backend (.NET 10 SDK 10.0.201 med `--solution`-flag på test) + frontend (Next.js 16, pnpm 9 med `packages: ['.']`-fix på workspace.yaml) parallella jobs + ci-aggregat. `deploy-dev.yml` (tag v*-dev) — OIDC assume + Docker build/push (SHA + tag-name) + describe→render→deploy-task-definition pattern + smoke-test mot https://dev.jobbpilot.se/api/ready med HSTS-verify (regression-skydd ADR 0027/TD-33). Worker no-wait under 14a-fönstret (PLACEHOLDER-creds → 14b fixar) + warning-output. **3 agent-reviews** parallellt (security-auditor + code-reviewer + dotnet-architect, alla APPROVE-with-fixes, 0 blockers, M1+M2+m2+n3+n1+MTPlatform-syntax+Worker-warn fixade pre-apply). Apply: 4 add (provider + role + policy + attachment). 4 commits (feat infra OIDC, ci workflows+reviews, fix CI iter 1, fix CI iter 2). **Build.yml mekaniskt verifierad** — alla pipeline-steps körs korrekt; Domain+Application+Architecture+Frontend gröna i CI. Backend Integration-tests fail i CI (88 errors `500 Internal Server Error` på /auth/register + 1 Worker partition-test) — **förexisterande issue, ej 14a-regression** (lokal körning passar 554/554). TD-37 ny (Integration-tests-CI-debug, addresseras vid 14b före first formal tag-deploy). | 2026-05-10 |
| STEG 13c | Fas 0 | **APPLIED 2026-05-10** (HTTPS smoke-test PASS på `https://dev.jobbpilot.se/api/ready` med HSTS-header). **Edge + DNS + HTTPS-flip** (ADR 0026 trigger 1 / TD-30, deadline 2026-06-08 — uppfylld 28 dagar i förväg). Domän `jobbpilot.se` registrerad hos STRATO (~80 kr/år). Två nya Terraform-modules: **`modules/route53/`** (apex hosted zone `Z028392711DGTDR1MGVC9` i prod/baseline som global delad resurs likt KMS) + **`modules/acm/`** (DNS-validerad cert med `aws_acm_certificate_validation` blocking-resource, timeouts.create=75m för svensk-registrar-marginal). dev-stack edit: `data "aws_route53_zone"` lookup + `module "acm_dev"` + A-ALIAS `dev.jobbpilot.se → ALB`. ALB-modul-edit: HTTP→HTTPS-redirect status_code 301 (AWS ALB hardlimited till 301/302; 308 ej implementerbart utan Lambda@Edge). **Sec-Major-2 fixad** (security-auditor): HSTS-implementation i Api/Program.cs — ny `HstsOptions` config-class + EnsureSafeForEnvironment-paritet med ForwardedHeadersConfig (dotnet-architect Viktigt-fynd 5), gate:at på `!IsDevelopment() && AlbOptions.HttpsEnabled`, FÖRE UseHttpsRedirection (förutsätter UseForwardedHeaders, dokumenterat). max-age=31536000 + includeSubDomains, preload defererat till prod-launch. **Sec-Major-1 dokumenterat** (DNSSEC defererad till Fas 1 via ADR 0027 med 4 trigger-villkor). 3 agent-reviews (code-reviewer APPROVE-with-fixes M-1 fixed timeouts.create=75m; security-auditor 0 Critical, 2 Sec-Major i ADR 0027, 3 Sec-Minor; dotnet-architect APPROVE-with-fixes Viktigt-fynd 5 inline). 5 nya TDs (TD-32 PQ-TLS-policy, TD-33 HSTS-pipeline-test, TD-34 DNSSEC Fas 1, TD-35 apex+www cert, TD-36 mTLS Fas 2). +17 tester (165/165 integration-gröna inkl. EnsureSafeForEnvironment Theory-cases). Apply: prod/baseline (1 add hosted zone) + dev (4 add ACM+CNAME+validation+ALIAS) + dev (4 add 1 change HTTPS-flip) — total ~3 min ECS rolling deployment. ADR 0026 → Superseded by ADR 0027. README.md tillagd separat (sober civic-utility-stil, 17 snabblänkar, 2 Mermaid-diagram). | 2026-05-10 |
| STEG 13b | Fas 0 | **APPLIED 2026-05-10** (Api smoke-test PASS via ALB; Worker restart-loop pga PLACEHOLDER-creds tills STEG 14). 4 in-flight kod-fixar krävdes: KMS-policy CloudWatch Logs grant, ASCII-only SG-descriptions, Dockerfile noble base-image (.NET 10 GA bytte default OS Bookworm→Noble), placeholder secret-versions med ignore_changes. Total apply 45 min. Spend ~$79/mån löper. Se `docs/sessions/2026-05-10-0730-steg13b-apply.md`. **Container-infra: ECR + IAM + CloudWatch + ALB + ECS Fargate** (Alt A2 andra block). Fem nya Terraform-modules: **`modules/ecr/`** — 2 separata repos (api, worker), KMS-encryption, scan_on_push, lifecycle keep-last-10. **`modules/cloudwatch_logs/`** — 3 LogGroups (api, worker, ecs-exec) med 30d retention + KMS (ADR 0024 D7). **`modules/iam_ecs/`** — execution-role + task-role-api + task-role-worker. Defense-in-depth via `aws:SourceAccount`-condition + `kms:ViaService`-restriction. Bedrock-policy attach via `data "aws_iam_policy"`-lookup. **`modules/alb/`** — internet-facing ALB, drop_invalid_header_fields, HTTP-listener default → target-group-api, HTTPS-listener gated på `var.https_listener_enabled` + `lifecycle.precondition` på acm_certificate_arn. **`modules/ecs/`** — Fargate cluster + Container Insights + capacity providers (FARGATE_SPOT default i dev = ~70% rabatt), task-defs (api: 0.5 vCPU + 1 GB med /api/ready-health; worker: 0.25 vCPU + 0.5 GB HTTP-fri per ADR 0023), services med deployment_circuit_breaker, autoscaling gated på var. Plus 2 Dockerfiles (multi-stage .NET 10, non-root `USER app`, no curl/HEALTHCHECK), `.dockerignore`, `/api/ready`-endpoint i Api/Program.cs (TODO TD-29: liveness ej readiness — fix vid Fas 2), `AlbOptions`-record i Api/Configuration. **Sec-Major-1 ADR-accepterad** via ADR 0026 (ALB HTTP-only Fas 0, 30d-tidsfönster deadline 2026-06-08, 5 triggers, mitigation-stack: rate-limiting + IP-anonymisering + audit-cascade + CloudTrail + restriktiv egress + DNS-disciplin). **Sec-Major-2 fixad in-block** — UseHttpsRedirection env-gate via `AlbOptions.HttpsEnabled` (matchande `var.alb_https_enabled` i Terraform → atomisk single source of truth med ALB-listener). **Kritisk Redis CS-mismatch fixad** (dotnet-architect-fynd) — komponerad CS i Terraform till single secret `ConnectionStrings__Redis = "host:port,password=...,ssl=True,abortConnect=False"` matchar `Infrastructure/DependencyInjection.cs:90` `GetConnectionString("Redis")`-pattern. Worker-konfig minimerad: ingen Redis (Worker använder inte Redis — verifierat). 4 agent-reviews (security-auditor approved Sec-Major-1+2 stängda, dotnet-architect OK, adr-keeper godkänd, code-reviewer approved 4 Minor varav M1+M2 fixade in-block). 3 nya TDs (TD-29 strict readiness Fas 2, TD-30 domänköp deadline 2026-06-08, TD-31 test för UseHttpsRedirection-gate). Cost vid apply: ~$79/mån (RDS $13 + Redis $8 + NAT $32 + ALB $16 + ECS $7 + ECR/CW $3). **Inte applied** — kräver `docker build/push` mellan IAM/CloudWatch/ECR-skapning och ALB/ECS-service-startup (annars `image_pull_failure`). | 2026-05-09 |
| Fas 1 Block A | Fas 1 | **KOD-FAS KOMPLETT 2026-05-11.** Fem sub-block A1+A2+A3+A4 + parallell TD-43 implementerade och pushade (commits 267e120 → 766a655). **A1 (TD-15 STÄNGD):** Resume-formulär aria-invalid + focus-flytt + aria-describedby per fält. fieldA11y-helper + pathToElementId + useEffect-focus i resume-content-form.tsx (16 fält). Stänger TD-15 från STEG 7b. **A2:** JobSeeker profil-edit-yta i (app)/mig/page.tsx — 6 nya filer (types/api/schemas/schemas-test/action/form) + 1 utökad page. RHF + Zod + Server Action, native select + native checkbox. A11y-pattern från TD-15-läxor återanvänd lokalt. Vitest 65→75 PASS (+10 schema-tester). **A3 (TD-31 STÄNGD):** UseHttpsRedirection env-gate anti-regression — 3 integration-tester via WebApplicationFactory + PostConfigure<HttpsRedirectionOptions> för middleware HTTPS-port-resolution i test-host. Abstract base + 3 sealed concrete factories (Disabled/Enabled-Production + Development). 554→557 PASS. **TD-43 (parallell session STÄNGD):** Komponent-tests för LoginForm + MeProfileForm + ResumeContentForm med Vitest + RTL + user-event. Pushad av separat CC-session, rebased mot main utan konflikter (disjoint filer). **A4 (TD-38 kod-fas KOMPLETT, apply väntar):** TLS-hardening med ConnectionStringFactory-split (ForMigrate: Trust=true för bootstrap; ForPersisted: SSL Mode=VerifyFull + Root Certificate). RDS global CA-bundle (~165KB, 108 certs) committat till `infra/certs/rds-global-bundle.pem` + COPY:as till `/etc/ssl/certs/` i Api/Worker Dockerfiles. .dockerignore-negation för bundle. 6 nya unit-tester i nytt `JobbPilot.Migrate.UnitTests`-projekt verifierar anti-regression (`ForPersisted` får ALDRIG ha Trust=true). 557→563 PASS. **9 reviews totalt** (3 per A2/A3/A4): code-reviewer + design-reviewer + dotnet-architect + security-auditor (A4 PRIO). Alla APPROVED med in-block-fixar för Major-fynd. **Stängda TDs:** TD-15, TD-31, TD-43. **Lyfta TDs:** TD-39 (error-summary), TD-40 (path-equality), TD-41 (Select-konvention), TD-42 (touch-target), TD-44 (HSTS-test), TD-45 (LoginForm focus), TD-46 (pathToElementId-export), TD-47 (bundle-rotation), TD-48 (architecture-test). **Apply-fas A4** dokumenterad i `docs/runbooks/td-38-tls-apply.md` (8-pkt security-checklist + rollback-procedur + CA-rotation-procedur) — väntar Klas-GO. Backend 563/563 + Frontend 75/75 + 3 nya komponent-tester PASS. | 2026-05-11 |
| STEG 14c | Fas 0 | **APPLIED 2026-05-10 ~21:55. FAS 0 STÄNGD.** TD-37 fix + first formal tag-deploy + Bootstrap-verify (3 av 3 14-block). **Block 1 (TD-37):** Root cause efter 6 commits (3b71fa5 → 8215658). Lokal repro med `ASPNETCORE_ENVIRONMENT=Production`-prefix bekräftade exakt samma 88 fail som CI. Initial fix-attempt (`UseEnvironment("Development")` + env-var i InitializeAsync) hade ingen effekt på CI eftersom ASP.NET-internal pipeline returnerade 500 utan exception. Debug-middleware (IStartupFilter echar 5xx + exception till stderr) + console-logger på Information-level för ASP.NET-internal categorier exponerade root cause: `StackExchange.Redis.RedisConnectionException` vid `JobbPilot.Infrastructure.DependencyInjection.AddIdentityAndSessions:131`. ApiFactory.ConfigureServices replacar `IDistributedCache` men INTE `IConnectionMultiplexer` (separat singleton med string captured vid registration-time). Lokalt funkar default localhost:6379 via Docker Compose; CI utan default Redis kraschar. **Fix:** sätt `ConnectionStrings__Redis` (+ Postgres för konsistens) som env-var i ApiFactory + StrictRateLimitApiFactory.InitializeAsync FÖRE Services-access (samma pattern som ProductionStartupFactory hade redan). Worker-test fixad parallellt: `DropPartitionsOlderThan_DropsOldPartitionsSkipsDefaultAndRecent` litade på migration-bootstrap-partitions som `RunAsync_EndToEnd` droppar (cutoff fixed-clock 2030-03-15 - 90d > bootstrap 2026-05-XX), test-ordering icke-deterministisk → fragil. Bytt mot self-managed recent-partition. Rate-limit-test-merge (delade budget-fix). Ny `ProductionStartupSmokeTests` — regression-skydd för Production-env-pipeline (UseEnvironment + ConnectionStrings + KnownNetworks=127.0.0.1/32 via env-var). build.yml `ASPNETCORE_ENVIRONMENT=Development` som runner-level säkerhet. Backend CI 554/554 grön. **Block 2 (tag-deploy):** Tag `v0.1.0-dev` på SHA `8215658` → deploy-dev.yml triggad. Initial fail på MCR transient (`mcr.microsoft.com/dotnet/aspnet:10.0-noble: 403`) → retry. Andra fail på `ecs:DescribeTaskDefinition AccessDeniedException` trots resource-scoped policy. **AWS-quirk:** `DescribeTaskDefinition` med family-namn (utan revision) loggar request som `*` så resource-scoped permission matchar inte → måste vara separat statement med `Resource: *`. Edit `modules/github_oidc/main.tf` + terraform apply mot prod-stacken (1 changed). Tredje retry → end-to-end PASS i 3m34s (OIDC + ECR push api+worker + ECS task-def render+deploy + smoke-test). Live verify: `curl https://dev.jobbpilot.se/api/ready` → 200 + HSTS. **Block 3 (Fas 0-stängning):** `aws iam list-users` → `Users: []` (Bootstrap-IAM-user verifierat tom — raderad i tidigare session). README badge `pre-MVP` → `Fas 1` + dev-live-badge. Fas-tabell: Fas 0 status `Pågår` → `Klar 2026-05-10`, Fas 1 `Planerad` → `Pågående`. Docs-sync (current-work + steg-tracker + tech-debt + session-logg). TD-37 stängd. **Stänger Fas 0** per BUILD.md §18. Spend ~$79.65/mån. | 2026-05-10 |

## 4. Mellan-arbete

> **Fryst 2026-05-17.** Denna tabell underhålls inte längre kronologiskt.
> Mellan-arbete från och med 2026-05-13 (F2-P8c och framåt) dokumenteras
> auktoritativt i `docs/current-work.md` och per-session i `docs/sessions/`.
> Posterna nedan bevaras som historik t.o.m. 2026-05-13 (F2-P8b) — de utökas
> inte. Skäl: enskild sanningskälla (DRY); session-loggarna är granskningstrail.

Cleanup-passningar, disciplin-uppgraderingar och dokumentations-arbete som inte hör till någon enskild STEG. Klas använder begreppet "Fas 0.x" för cleanup-arbete mellan officiella faser.

| Period | Beskrivning | Källor | Status |
|--------|-------------|--------|--------|
| 2026-05-07 | Upptakt: ADR 0019 etablerad (solo direct-push), CLAUDE.md uppgraderad (§9.4 discovery, §9.5 web-search, §9.2 utökad), tech-debt.md etablerad, hook-vakt fix:ad för Agent SDK-läget, precompact-rapporter exkluderade från versionshantering | Webb-chats: ADR 0019-chatt + Moment 1-5-chatt | Pågående (Moment 5 = denna tracker) |
| 2026-05-11 efm | **Väg C Fas 1.5 housekeeping** efter Fas 1-milestone-stängning. 4 TDs stängda i samlad batch: TD-55 (PagedResult retro-fit, runtime typ-skew identifierat + stängt), TD-48 (Mono.Cecil arch-test för Trust=true-läckage), TD-47 (GH Actions cron för CA-bundle-rotation), TD-50 (admin-bootstrap runbook). 6 commits (`c2f539e` → `a9ca126`). CTO-beslut entydigt motiverat: Alt 3 commit-strategi + Alt A2 Mono.Cecil + seriell A→B→C-ordning. 2 agent-reviews APPROVED med alla 3 Minor in-block-fixade. Ny TD-56 (ListJobAds Fas 2-paginering) lyft som scope-deferral. Backend 594/594 + Frontend 150/150 grönt. | docs/sessions/2026-05-11-1200-vag-c-fas15-housekeeping.md | ✓ Klart |
| 2026-05-11 sen efm | **Väg B a11y-pass** efter Väg C-housekeeping. 2 TDs stängda: TD-54 (text-text-tertiary kontrast-fix för funktionell text, WCAG AA 1.4.3 — 16 träffar i 9 filer, kontextuell mapping funktionell→secondary + dekorativ→tertiary kvar) + TD-42 (touch-target-uppgradering till skill-doc-defaults: h-9 default + h-11 critical CTAs + h-9 icon + h-11 icon-lg). 4 commits (`8cfbde4` → `1b0b9ec`). Skill-doc-konformitet snarare än multi-approach-val: kod-baseline var drift från `jobbpilot-design-components` + `jobbpilot-design-a11y`-skills. 4 agent-reviews APPROVED (code-reviewer + design-reviewer × 2 block) med 3 Major in-block-fixade per 4h-regel (cursor-not-allowed pagination disabled + Avbryt-button h-7→h-9 + page-header CTAs sm→default). Ny TD-57 (native form-controls divergerar från Input-primitive — datetime-local + native select rounded-md vs rounded-sm, saknar aria-invalid/dark-mode/disabled bg) lyft som M3-deferral (kräver design-beslut). Backend 594/594 + Frontend 150/150 grönt. | docs/sessions/2026-05-11-1400-vag-b-a11y-pass.md | ✓ Klart |
| 2026-05-11 ~15:30 | **Väg E TDs-cleanup** efter Väg B-a11y-pass. 2 TDs stängda: TD-40 (leaf-path regression-bevakning för `resume-schemas` refines — 3 nya Vitest-tester som kontraktslåser kompatibilitet mellan schema-refines `path: ["endDate"]` och `pathToElementId` routing-helper) + TD-49 (docs-only-stängning — discovery fann att `HstsOptionsTests.cs` redan täcker alla 6 TD-49-cases sedan STEG 13c; dotnet-architect-reviewen som lyfte TD-49 letade efter `JobbPilot.Api.UnitTests/`-projekt istället för befintlig `JobbPilot.Api.IntegrationTests/Configuration/`-pattern). 2 commits (`6b8f087` → `954fe1e`). Test-only + docs-only — inget produktionskod-touch. 1 agent-review (code-reviewer) APPROVE med Minor in-block-fixad per 4h-regel (`findIssueAtPath`-helper söker via path-prefix istället för message-string-match så framtida copy-tweaks inte rödnar testet). Backend 594/594 + Frontend 150 → 153 grönt. **Lärdom:** TD-skapande ska verifiera test-existens via grep + Glob över ALLA test-projekt, inte anta projekt-namn. | docs/sessions/2026-05-11-1530-vag-e-tds-cleanup.md | ✓ Klart |
| 2026-05-11 ~16:30 | **Fas 2 polish-block — arch-audit Minor/Nit-fixar.** Klas valde Alt 2 efter arch-audit Fas 1 Discovery (0 Blocker / 0 Major / 4 Minor / 3 Nit). 5 audit-fynd fixade in-block, 4 TDs lyfta för defererade. **Block A:** N-1 events upprade på Application.SoftDelete + JobSeeker.SoftDelete (CTO Riktning A — domain events som historiska fakta per Evans/Vernon) + N-3 DomainException i Domain.Common med Resume.MasterVersion-guard (CTO Alt A — dependency rule §2.1 + OCP) + H-4 PageNumber→Page rename i 2 queries + arch-test strikthet. **Block B:** N-2 IdempotentAdminRoleSeeder env-gated catch via IHostEnvironment (CTO Alt A — §3.4 fail-loud + Twelve-Factor §10 dev/prod parity) + 5 unit-tests + anti-regression-test som bevisar prod-bubble. **Block C:** H-3 role-fetch flyttad till SessionRoleClaimsTransformation (IClaimsTransformation) + sentinel-claim för idempotency + arch-test för IClaimsTransformation-konsument-allowlist. **Agent-reviews:** 5 reviews (CTO×3, dotnet-architect×2, code-reviewer×2, security-auditor×2). 3 Major fixade in-block (Block B test-fixture-removal-blindzon, Block C CancellationToken-läckage, Block C idempotency-guard-bug). Alla Minor fixade in-block per 4h-regel. **TDs lyfta:** TD-58 H-1 IAccountHardDeleter ISP-split (Fas 6) + TD-59 H-2 ICurrentJobSeeker-port (Fas 6 impersonation) + TD-60 ADR auth-pipeline-ordning + TD-61 audit-trail-evidence-test. Backend 594 → 607 (+13 tester). 4 commits pushade (`ff3704f`, `a683ae1`, `35b9dc0`, `c0ada25`). | docs/sessions/2026-05-11-1630-fas2-polish-block.md | ✓ Klart |
| 2026-05-11 ~17:00 | **Stationär Väg A — TD-60 ADR 0029.** Stationär-CC-session efter Fas 2 polish-block-pushen. ADR 0029 (HTTP-auth-pipeline + IClaimsTransformation-disciplin, komplementär till 0028 — supersedas inte) levererad + 5 integration-tester i `SessionRoleClaimsTransformationTests`. TD-60 stängd. 1 commit (`f4a1569` docs + tester bundlat per CTO-rek REP/CCP). 2 agent-reviews + 1 CTO-triage; 0 nya TDs lyfta — alla Major/Minor fixade in-block per 4h-regel inkl. M-2 (falsk test-coverage-claim) löst via Alt B integration-test-strategi. Backend 607 → 612 grönt. Scope: ~2.5h (vs original 45 min docs-pass — agent-review-driven utökning). | docs/sessions/2026-05-11-1700-vag-a-td60-adr-0029.md | ✓ Klart |
| 2026-05-11 ~19:00 | **Stationär Väg B — TD-61 audit-trail-evidence.** Stationär-CC-session efter Väg A pushad. **Discovery avslöjade att TD-61:s ursprungspremiss var provably false** — seedern skriver INTE till `AuditLogEntries`-tabellen (ADR 0022 §Kontext rad 11 bekräftar: IAuthAuditLogger skriver bara strukturerad logg). senior-cto-advisor valde Alt A (korrigera XML-doc + integration-test mot ILogger-sink) över Alt B (ADR 0022-port-erosion) eller Alt C (defer). Motiverat mot Martin 2017/2008 (SRP, Clean Code kap. 4 lögnaktiga kommentarer), Fowler 2018 (Refactoring code smells), Ford/Parsons/Kua 2017 (fitness functions skyddar portar), Cohn 2009 (Test Pyramid), Twelve-Factor §XI. **Leverans:** XML-doc rad 17-31 korrigerad (ärlig: observability via `LogAdminAssigned` EventId=2 → ILogger → Seq/CloudWatch + anti-claim att AuditLogEntries INTE skrivs + ADR 0022/Fas 6-hänvisning) + 3 testfall i `IdempotentAdminRoleSeederAuditEvidenceTests` (happy path EventId=2 / idempotens EventId=3 / saknad user EventId=4) via AddIdentityCore + UseInMemoryDatabase + custom CapturingLogger. **2 agent-reviews APPROVED:** code-reviewer (3 Minor + 1 Nit) + dotnet-architect (2 Minor + 1 Nit). M-1 (PascalCase test-namn per §3.2) + M-2 (non-generic CapturingLogger) in-block-fixade per 4h-regel. TD-61 stängd. 0 nya TDs lyfta. Backend 612 → 615 grönt. Scope: ~2h (vs original 1h — discovery + CTO-triage utvidgning). Pending: 1 commit + push efter Klas-diff-granskning. | docs/sessions/2026-05-11-1900-vag-b-td61-audit-evidence.md | Väntar Klas-GO |
| 2026-05-12 kväll | **F2-P7 + P8a + bootstrap + aggregate-review.** 17 commits — JobAd-paginering med PagedResult (F2-P7, TD-56 stängd) + ExternalReference VO + JobAd.Import + EF migration (F2-P8a) + JobbPilot.Migrate CLI-mode-dispatch + Phase E (ADR 0033) + bootstrap-mode för Identity-schema-init (ADR 0034) + auto-trigga schema-task i deploy-dev.yml. Tag `v0.2.1-dev` LIVE på dev. 3 nya ADRs (0032 JobTech-integration + 0033 Migrate-modes + 0034 DB-role-privilege-separation). 4 parallella post-hoc reviewers + 4 CTO-ronder (M1+M2 in-block-fixade). 3 nya TDs (TD-72 trigger / TD-73 P8c-gating / TD-74 opportunistic). 6 deploy-iterations + DRY-refactor till MigrationsOptionsFactory. Backend 502 → 522 grönt. **Disciplinmiss fångad:** reviewers post-hoc istället för inline → 4 fix-commits. | docs/sessions/2026-05-12-2110-f2-p7-p8a-bootstrap-aggregate-review.md | ✓ Klart |
| 2026-05-13 natt | **F2-P8b JobTech Infrastructure-leverans.** 5 commits — IJobSource Application-port + Refit-baserad IJobTechSearchClient + typed IJobTechStreamClient + PlatsbankenJobSource + JobTechPayloadSanitizer (TD-73 punkt 1) + Microsoft.Extensions.Http.Resilience + custom Polly-pipeline för Stream (RateLimiter → Retry → CB) + admin-trigger-endpoint POST /api/v1/admin/job-ads/sync/platsbanken + gdpr-processing-register.md (TD-73 punkt 3) + AdminBootstrap-Terraform-sync. Tag `v0.2.2.1-dev` LIVE på dev (efter `v0.2.2-dev`-fail pga global.json `rollForward=latestPatch` för strikt mot container-SDK 10.0.300 → fixad till `latestFeature`). **Reviewers INLINE per CLAUDE.md §9.2** (disciplin-fix från F2-P8a) — dotnet-architect INNAN kod + code-reviewer/security-auditor INNAN commit, alla Major + Minor in-block-fixade. **Klas-fynd via curl:** JobStream 2.1.1 har deprecated v1-paths → v2 path-migration (commit 8d89ded + 139a85e) + v2-shape (webpage_url ersätter source_links + 3 nya v2-PII-regression-tester). **CTO-rond 5:** ALB-timeout-beslut Variant B (acceptera 504, vänta P8c) — synkron HTTP är fel transport för 50-100 MB snapshot (Martin 2017 Boundaries + Fowler 2002 Async Messaging). Pipeline-mekanik verifierad i CloudWatch (CT-propagation OK, no partial-persist). Backend 522 → 528 grönt (273 Application + 218 Domain + 37 Architecture). **TD-73 progress:** punkt 1 + 3 levererade; punkt 2 + 4 kvar i P8c-acceptance-criteria som planerat arbete (inte TD per §9.6). | docs/sessions/2026-05-13-0600-f2-p8b-jobtech-infra.md | ✓ Klart |

## 5. Aktuellt

> **Fryst 2026-05-17.** Aktuell status underhålls auktoritativt i
> `docs/current-work.md` (status-header + "Active now"). Innehållet nedan
> speglar läget 2026-05-13 (F2-P8b) och uppdateras inte. Se current-work.md
> för nuläge.

**STEG-fokus:** **F2-P8b KOMPLETT 2026-05-13.** JobTech-integration Infrastructure-lager levererat: IJobSource Application-port + Refit JobSearch-klient + typed JobStream-klient + PlatsbankenJobSource + sanitizer + admin-trigger-endpoint. Tag `v0.2.2.1-dev` live på `https://dev.jobbpilot.se/api/ready` (HTTP 200). Backend 528 tester gröna. 3 ADRs (0032 + amendments + 0033 + 0034) etablerade. TD-73 punkt 1 + 3 levererade.

**Nästa:** **F2-P8c (Hangfire-jobben).** Per ADR 0032 §9: `SyncPlatsbankenStreamJob` (cron `*/10`) + `SyncPlatsbankenSnapshotJob` (cron `0 2`) + `UpsertExternalJobAdCommand` med DbUpdateException-pattern + `PurgeStaleRawPayloadsJob` (TD-73 punkt 2) + Removal-handling via JobAd.Archive + JobAdsSyncedDomainEvent audit-wire + right-to-erasure-cascade (TD-73 punkt 4). E2E-DB-persist-verifiering ingår i P8c-acceptance-criteria.

**Tidigare STEG-fokus (historiskt):** FAS 1 BLOCK A KOD-FAS KOMPLETT 2026-05-11.

**STEG 7a** (Resume-aggregat backend): Komplett 2026-05-08.

**STEG 7b** (Frontend /cv): Komplett 2026-05-08.

**STEG 8** (Audit log-infrastruktur): Komplett 2026-05-08. Pipeline-behavior + marker-interface (ADR 0022). 10 commands i Application+Resume märkta `IAuditableCommand<TResponse>`. Audit-rad och data-mutation persisteras atomiskt i samma SaveChanges. +41 tester. TD-9 stängd, TD-16 ny.

**STEG 9** (Worker-pipeline + Hangfire): Komplett 2026-05-08. ADR 0023. Worker-aktivering + Hangfire 1.8.23 + Hangfire.PostgreSql 1.21.1. DI-modulär refaktor. Stale-detektering på Application-aggregatet (`LastStatusChangeAt` + `GhostedThresholdDays` per BUILD.md §schema). `DetectGhostedApplicationsJob` orchestrator. **Kritisk fångst:** Pipeline-bug där `MediatorPipelineBehaviors.InOrder` inte registrerade behaviors via `options.PipelineBehaviors` → tyst data-loss. Fix: `AddMediatorPipelineBehaviors()` open-generic DI. Newtonsoft.Json 13.0.3 transitiv CVE-pinning. +32 tester (9 Domain + 12 Application + 5 Architecture + 6 Worker SmokeTest). TD-17 ny (Hangfire prod-härdning, blocker för Fas 1 prod-deploy). TD-18 ny (intervju-states-utökning). TD-19 ny (defense-in-depth-förbättringar för Worker-jobb).

**Spec-drift fångad:** STEG 9-startprompt sade "ghosted_threshold_days per JobSeeker (default 21)" men BUILD.md §schema rad 715–727 specificerar **per Application**. Klas valde BUILD.md-versionen (DDD: Application-aggregatet äger sin tids-semantik). Steg-tracker uppdaterad ovan.

**Plan-design-modell:** STEG 9 körde plan-design via CC + dotnet-architect-validering (utan webb-Claude). Fungerade för väl-avgränsad infrastruktur när ADR 0022-spec fanns delvis färdig. Webb-Claude behövs inte för upprepningsmönster eller infrastruktur som har ADR-stöd.

**Test-strategi-validering:** Klas tillägg #3 (integration smoke-test framför manuellt smoke-test) bevisade sitt värde — manuellt smoke-test hade missat pipeline-bug-fyndet. Mönster: alla nya orchestrator-/Worker-jobb ska ha integration smoke-test med `[Trait("Category", "SmokeTest")]`.

**STEG 10a** (Audit-retention via partitioning): Komplett 2026-05-08. ADR 0024 D1+D2. `audit_log` partitionerad daglig. `AuditLogRetentionJob` registrerad i Hangfire 03:00 UTC. Komposit-PK `(id, occurred_at)`. **Lärdomar fångade:** PK-constraint följde inte med RENAME (fix: `ALTER TABLE … RENAME CONSTRAINT`); EF Core 10 PendingModelChangesWarning kräver `ValueGeneratedNever` på alla komposit-PK-kolumner; SqlQueryRaw + format-string tolkar `[0-9]{8}` som `{8}`-placeholder (fix: escape till `{{8}}`, smoke-test fångade). +7 tester (3 arch + 4 smoke). TD-16 del 1 stängd. TD-20 ny (defensiv refactor defererad).

**STEG 10b** (DELETE /me + Art. 17-cascade): Komplett 2026-05-08. ADR 0024 D3-D6. Två CC-design-frågor besvarade: secondary Redis-set för InvalidateAllForUserAsync + ny IAppDbContext-injektion i LoginCommandHandler. Säkerhets-fix från audit: returnera `Auth.InvalidCredentials` istället för `AccountPendingDeletion` för att undvika info disclosure (GDPR Art. 32). `AddCoreIdentityForWorker`-extension löser cross-context-fråga (Worker behöver UserManager utan HTTP-bagage). +17 tester totalt. TD-16 stängd. TD-21-25 nya (varav TD-21+TD-22 är Fas 1 prod-blockare).

**STEG 11** (Fas 1 prod-blockare-cleanup): Komplett 2026-05-09. Tre block i ordning TD-22 → TD-17 → TD-21, alla med parallella security-auditor + code-reviewer reviews per CLAUDE.md §9.2. **TD-22:** ADR 0024 D7 (App-logg-redaction + 30d retention). `IIpAnonymizer`-port lyft från privat metod, konsumeras nu av både audit-pipeline och AuthAuditLogger. **TD-17:** 5/6 punkter stängda — config-driven Hangfire-options med allow-list production-defense, Fargate SIGTERM 25s+3s timeout-kedja, runbook med GRANT-modell + REVOKE PUBLIC + 8-punkts dashboard-checklista. Punkt 4 (ConnectionStrings split) defererad till Fas 0-stängning. **TD-21:** rate-limiting på DELETE /me (1/60s per UserId med NoLimiter för anonymous) + auth-endpoints (20/min auth-write OWASP-CGN-kompatibel, 30/min auth-loose). UseForwardedHeaders + OnRejected med LoggerMessage source-gen + Retry-After (RFC 6585). Separat StrictRateLimitApiFactory för isolerad 429-integration-test. **Nya TD:** TD-27 (EmailHash-HMAC Fas 2), TD-28 (frontend typed-confirmation-UX). +27 tester. Backend 502 totalt.

**Lärdomar STEG 11:**
- `IConfiguration.GetSection().Get<>()` direkt vid Program.cs-startup istället för `IOptions<>` är OK när värdet bara läses vid host-uppstart — håller direct-binding-elegansen
- Allow-list production-defense (`IsDevelopment\|\|IsEnvironment("Test")`) > pure `IsProduction()` så Staging/Preprod/Demo inte tappar skydd
- xUnit `parallelizeTestCollections=false` krävs när två separata fixturer delar process-globala env-vars (här: rate-limit-overlay)
- Hangfire.AspNetCore drar in Microsoft.AspNetCore.* — bryter mot ADR 0023 Worker HTTP-fri-disciplin. Trim defererad till TD-19 Fas 2.
- LoggerMessage source-gen är obligatorisk under CA1848 i JobbPilot — `_logger.LogWarning(...)` i hot path bryter build

**STEG 12** (kod-pre-launch-gates): Komplett 2026-05-09. Alt A1 av Klas:s A4-sekvens (A1→A2→A3 för Fas 0-stängning). Tre block: Worker HangfireStorage-fallback (TD-17 punkt 4), Api ForwardedHeadersConfig + production-defense (TD-21 KnownNetworks), båda `appsettings.Production.json` overlays. **Sec-Major-1** (allow-list-symmetri-miss mellan Worker `safeForAutoSchema` och Api KnownNetworks-tomt-array) fixad in-block via lyft till testbar `EnsureSafeForEnvironment(envName)`-metod på `ForwardedHeadersConfig`. **Sec-Major-2** (vilseledande overlay-kommentar om CloudFront) docs-fixad — runbook §3.3 förtydligad om AWS-managed prefix-list för CloudFront edge-IPs. +35 tester (537 backend-totalt). Reviews: 0 Critical/Major-blocker. M1-M3 fixade in-block.

**STEG 13a** (Terraform dev-stack — networking + databas + cache): Kod-skriven 2026-05-09 (ej applied). Tre nya `modules/{network,rds,redis}/` + ny `environments/dev/`-stack. VPC `10.0.0.0/16` 3-AZ, single NAT (cost-optimized), VPC Endpoints (S3+SecretsManager+KMS, Bedrock utelämnad pga region-mismatch). RDS Postgres 18.3 Multi-AZ + KMS-encryption end-to-end + AWS-managed master-secret. ElastiCache Valkey 8.0 (BUILD.md §15.1 sa "Redis 8.6" men Redis 8.x är post-license-byte och inte AWS-supportad — Valkey är AWS:s Redis-kompatibla efterföljare). Två dev-secrets-placeholders för STEG 14 DDL-värde-population. **Sec-Major-1 + Sec-Minor-6 fixade in-block** via RDS parameter-group (`log_statement=none` hindrar STEG 14 password-leak; `log_parameter_max_length=0` trunkerar bind-värden så slow-query-log inte exponerar PII; explicit `aws_cloudwatch_log_group` med 30d retention uppfyller ADR 0024 D7). **Sec-Major-2 ADR-accepterad** via ADR 0025 — ECS-egress `0.0.0.0/0` Fas 0-acceptance med dokumenterad mitigation-stack + omvärderingstrigger Fas 1→Fas 2 + förberedd hardening-väg. 5 Minor + 3 Nit defererade till STEG 13b/staging. **Inte applied** — kräver SSO-login + budget-höjning ($50→$200) + version-verifiering (Valkey/PG family-strängar via AWS API) innan `terraform apply`.

**STEG 13b** (container-infra): Komplett 2026-05-09 (kod, ej applied). Fem moduler + Dockerfiles + Api/Program.cs-edit. ADR 0026 + 4 agent-reviews. **Sec-Major-1 ADR-accepterad** med 30d-tidsfönster + 5 triggers. **Sec-Major-2 fixad** via UseHttpsRedirection env-gate (single source of truth med Terraform-variabel). **Kritisk Redis CS-mismatch upptäckt av dotnet-architect** — fix: komponera CS som single Secrets Manager-secret. Worker-konfig minimerad (ingen Redis). 3 nya TDs.

**STEG 13c** (Edge + DNS + HTTPS-flip): Komplett 2026-05-10. Två nya Terraform-modules (route53 + acm) + ALB 308→301-fix (AWS-API-begränsning upptäckt mid-apply) + HSTS-implementation i Api. ADR 0027 supersedar 0026. README.md tillagd. 165/165 integration-tester gröna. 5 nya TDs.

**Lärdomar STEG 13c:**
- **AWS ALB `RedirectActionConfig.StatusCode` är hardlimited till HTTP_301 | HTTP_302.** 308 (Permanent Redirect, POST-method-bevarande) är inte implementerbart utan Lambda@Edge eller CloudFront-rewrite. Validering fångas inte av `terraform validate` (AWS-API-side). Lärdom: vid AWS-resource-fält som tar enum-strängar — kolla AWS-docs INNAN code-review eftersom `terraform validate` ger false-positive. 301 räcker i praktiken eftersom HSTS+browser-cache gör POST-anrop går direkt till HTTPS efter första GET.
- **STRATO-NS-propagering snabbare än varnat.** STRATO-UI varnar "upp till 24h" men `.se`-zonen propagerade inom ~30 min (Resolve-DnsName mot 8.8.8.8/1.1.1.1/9.9.9.9 visade AWS-NS efter NS-edit). Lärdom: använd `aws_acm_certificate_validation.timeouts.create` med marginal (75m) men förvänta snabbare i praktiken. Worst-case scenario sällan worst-case.
- **`aws_acm_certificate_validation`-resursen är idempotent vid timeout.** Cert står PENDING_VALIDATION upp till 72h innan AWS auto-deletar. Re-apply efter timeout plockar upp där den slutade — inga recreates, inget förlorat $$. Detta gör "apply nu i background och re-applya vid timeout" till en säker strategi.
- **HSTS-pipeline-ordning kritiskt:** `UseForwardedHeaders → UseHsts → UseHttpsRedirection`. Bakom ALB är `Request.IsHttps` annars false → HSTS-headern sätts aldrig. dotnet-architect-fynd som behöver dokumenteras i kod-kommentar för att framtida refactors inte ska bryta ordningen.
- **Cross-stack data-lookup-pattern (data "aws_route53_zone")** följer KMS-master-key-pattern från STEG 13a. Ren state-isolation; dev-stack vet inget om prod/baseline-state. Bra blueprint för framtida delade resurser (ACM-cert för apex i prod när lansering närmar sig, etc).
- **Klas's existing README-mall (Tokyo Night-tema) passade inte civic-utility-position.** README skrevs i sober stil — inga animerade SVGs, inga emojis i prosa, två Mermaid-diagram för arkitektur-läsbarhet, 17 snabblänkar för skim-navigering. AI-läsbarhet säkrad via semantiska headers + tabellformat.

**Lärdomar STEG 13b:**
- **Konfig-mismatch mellan IaC och .NET-konsumtion är klassisk fotgropfälla.** Initial impl injicerade Redis Host/Port/AuthToken som 3 separata secrets/env-vars. Infrastructure-koden läste `GetConnectionString("Redis")` som single string. Hade inte upptäckts före första prod-uppstart om dotnet-architect inte invocerats parallellt med security-auditor. Lärdom: vid IaC-konsumtion av .NET-config — verifiera mot existerande config-läsning innan plan-design.
- **Fargate ignorerar Docker `HEALTHCHECK`-direktiv** (per AWS-docs — bara EC2-launch-type respekterar). Container-level HEALTHCHECK i image är meningslöst overhead för Fargate-tasks. Använd ECS task-def `healthCheck`-block ELLER ALB target-group health-check. Vi valde target-group + container exit code som dubbel-täckning.
- **`UseHttpsRedirection()` × HTTP-only-ALB är fail-loud-falla** — redirect 307→443 mot stängd port → ALB target-group failer → ECS deployment_circuit_breaker rollback. Konfigdriven gate via env-var som flippas synkront med ALB-listener-config = atomisk konsistens.
- **AWS-managed prefix-list saknas för Bedrock-egress** — cross-region-trafik från eu-north-1 → eu-central-1/eu-west-1 går via NAT eftersom Bedrock-tjänsten inte har region-prefix-list. Validerat i ADR 0025 + ADR 0026 hardening-väg.
- **`lifecycle.precondition` är fail-fast vid plan-tid** (Terraform 1.x+) — kraftfullt mot operatör-misstag som "https_listener_enabled=true men acm_certificate_arn=null". Failar i plan istället för i apply.

**Lärdomar STEG 13a:**
- **Cost-policy: dev = deploy-pipeline-verifierare, inte produktions-mirror.** Initial implementation defaultade till BUILD.md §15.1:s prod-spec (db.t4g.medium Multi-AZ + cache.t4g.small × 2 + Interface Endpoints) → ~$140/mån för dev. Klas:s prio (kostnadskontroll dag 1) kräver lean-dev: t4g.micro Single-AZ + t4g.micro × 1 + S3-only-endpoint → ~$30/mån. Multi-AZ + replicas testas först i staging/prod. Modul-defaults är lean; staging/prod sätter Multi-AZ explicit.
- **`monthly_budget_usd` är alert-tröskel, inte spending-cap.** Att höja $50→$200 sänker inte spending — det fördröjer alarmet. Rätt strategi vid kostnadskontroll: sänk faktiska resurser, behåll låg alert-tröskel för tidig varning.
- BUILD.md §15.1 "Redis 8.6" är spec-drift mot AWS-verklighet — Redis 8.x är Redis Inc:s post-license-byte-version som AWS inte adopterat. Valkey 8 är AWS:s Redis-kompatibla efterföljare. Justera BUILD.md vid en lämplig docs-pass.
- RDS `log_statement="ddl"` är klassisk fotgropfälla vid Postgres-tuning — DDL-statements innehåller `CREATE/ALTER ROLE PASSWORD '...'` i klartext. Sätt `none` i prod, byt till `mod` bara om DDL-audit verkligen behövs (och då via en pattern som strippar passwords pre-execution).
- `log_parameter_max_length=0` är icke-trivialt rätt — `log_min_duration_statement` ensam är inte PII-säker eftersom slow-query-textens bind-värden loggas. Krävs Postgres 13+.
- AWS-managed CloudWatch LogGroups skapas implicit av RDS/ECS/Lambda med default `Never expire` — alltid deklarera explicit `aws_cloudwatch_log_group` *före* tjänst-skapning för att kontrollera retention + KMS.
- Cross-region Bedrock från eu-north-1 fungerar via NAT Gateway, men ingen lokal VPC Endpoint finns för Bedrock i regioner där tjänsten inte är hostad.
- `data "aws_kms_alias"`-lookup mellan stackar är cleanare än `data "terraform_remote_state"` — inga tfstate-permissions att hantera, ingen koppling till baseline-stackens version.

**Lärdomar STEG 12:**
- .NET 10 `Microsoft.AspNetCore.HttpOverrides.IPNetwork` är deprecated → använd `System.Net.IPNetwork.TryParse` + `ForwardedHeadersOptions.KnownIPNetworks` istället
- Allow-list-pattern (Worker `safeForAutoSchema` + Api `EnsureSafeForEnvironment`) ska replikeras strukturellt mellan parallella entry-points — symmetri-miss → tyst no-op-yta i prod
- ForwardLimit > 1 kräver att ALLA hops är i KnownNetworks/KnownProxies — bara VPC-CIDR räcker inte vid CloudFront+ALB-kedja eftersom CloudFront edge-IPs ligger i AWS-managed prefix-list, inte VPC
- ASP.NET JsonConfigurationProvider stödjer `// comments` sedan .NET Core 3.0 (`JsonReaderOptions.CommentHandling = Skip`) — användbart för load-bearing pre-launch-gate-dokumentation i overlays
- Statisk testbar metod på options-klassen > inline-Program.cs-logik så uppstart-validering kan unit-testas utan host-bygge

**Nästa:** STEG 12 — kräver beslut. Se §6.

För session-detaljer och commit-historik, se `docs/current-work.md`.

## 6. Nästa STEG

**STEG 12 — kräver beslut.** Tre primära kandidater:

**Alt A — Fas 0-stängning (rekommenderad efter STEG 11)**

Applicera STEG 11:s pre-launch-gates och göra första prod-deploy:
- Första deploy till dev.jobbpilot.se via GitHub Actions tag-pipeline (BUILD.md §15.3)
- AWS-konfig:
  - CloudWatch LogGroups retention=30 (TD-22 / aws-setup.md §3.2)
  - ALB ForwardedHeaders KnownNetworks=VPC-CIDR (TD-21 / aws-setup.md §3.3)
  - ConnectionStrings split: jobbpilot_app + jobbpilot_worker AWS Secrets Manager (TD-17 punkt 4 / hangfire-schema.md §4)
  - Hangfire schema-DDL via Install.sql körd av jobbpilot_migrations-roll (hangfire-schema.md §3)
  - REVOKE ALL ON SCHEMA hangfire FROM PUBLIC innan GRANT (Sec-Major-2 STEG 11)
- Bootstrap-IAM-user raderad (aws-setup.md §3.4)
- appsettings.Production.json med:
  - `Hangfire:PrepareSchemaIfNecessary: false`
  - `Hangfire:ShutdownTimeoutSeconds: 25`
- security-auditor invokeras vid Fas 0-block (deployment + IAM-cleanup)

**Alt B — Fortsätt Fas 1-features**

Per BUILD.md §18 milstolpe "manuell CV + 'fake' ansökningar":
- Application Management UX-pass (Resume-version-Tailored, status-flöde-polish)
- Andra Fas 1-features

**Alt C — TD-19 Worker defense-in-depth-cleanup (mindre scope)**

- Hangfire.AspNetCore → Hangfire.NetCore (Worker HTTP-fri-disciplin per ADR 0023)
- Architecture-test-utökning till allow-list-pattern
- Worker-orchestrator max-batch-size-guard

**Min rek:** Alt A naturlig efter STEG 11 — alla pre-launch-gates dokumenterade, Klas applicerar dem operativt och får första prod-miljö. Vid Alt B finns risk att STEG 11:s dokumentation åldras innan den används.

**Status:** Behöver beslutas av Klas.

## 7. Numreringsfotnot

Faktisk historisk numrering följer projektets utveckling, inte BUILD.md §18:s fas-indelning. Sammanfattning:

- **Sessions 1-5:** pre-STEG infrastrukturarbete. Hör till Fas 0.
- **STEG 1-2:** kärnkod-grundläggning (.NET solution, JobAd domain). Hör till Fas 0.
- **STEG 3+:** post-bootstrap arbete. Hör delvis till Fas 0 (frontend bootstrap STEG 4a — design system-baseline) och delvis till Fas 1 (auth + Core Domain).
- **STEG 4a/4b sub-numrering:** "a/b/c"-suffix används när ett STEG sträcker sig över flera sessioner med substantiellt distinkt scope.
- **Moment-numrering** (1-5 i upptakter): separat axel för mellan-arbete, inte STEG.

Renumrering har övervägts och avvisats — bryter audit-trail mot commits, sessions/-loggar och ADR-referenser.
