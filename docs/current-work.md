# Current work — Jobbliggaren

**Status:** **LOGO-ÖVERSYN FAS 1 — SIGILLET-MARKEN LEVERERAD 2026-06-13 (ADR 0070). Den 4-uddiga kompassen ersatt av ett fyllt civilt registersigill: smooth grön skiva (`--jp-accent-800` `#15603F`) + tunn vit inre ring + 3 liggar-rader, mittenraden guld (`--jp-gold` `#E8C77B`) med bock = loggad post — semantiskt knutet till namnet (liggare = register) + `.jp-table--flat`. Aktiverar `--jp-gold`-tokenen ADR 0068 reserverade. `BrandMarkSvg` 2→3-fyllskontrakt (primär/accent/papper via `--jp-mark-*`, tema-stabila), `brand-logo` `currentColor`→tokens, wordmark navy→ink (+ dark scoped-vit-topbar-override `#133F73`→`#0C1A2E`). Ytor: icon.svg + apple-icon + opengraph-image + twitter-image (satori runtime-renderade & verifierade) + manifest `theme_color` navy→grön `#15603F`. Navy-kompass + `#FFCD00`-prick (`--jp-brand-accent`) pensionerade; navy-rampen konsumentlös → F-städ. ADR 0070 (ny, delvis supersederar ADR 0068 Beslut 1:s logo-not) + DESIGN.md §11/rad 66 + design-skills (principles regel 5, tokens-skill + refs) synkade. Gates: tsc rent · vitest 868/868 · eslint 0 errors · `next build` grön · satori OG 1200×630 + apple 180×180 runtime-renderade. Reviews: code-reviewer ✓ Approved (0/0/3 Minor — fixade) + design-reviewer ⚠→löst (motionen GODKÄND; Major 1 spec-drift FIXAD; Major 2 spinner-utan-konsument = HÅLLEN per Klas-beslut → Fas 2 följ-PR). `pnpm visual-verify` kunde EJ köras i container (Playwright Chromium-CDN blockerad av nätpolicyn) → header-lockup light/dark + scoped-topbar-theming PENDING Klas stack (formell screenshot-grind). NÄSTA = SPINNER FAS 2: `BrandSpinner` ("Sigillet i rörelse" — gold arc + pulserande rader, ren CSS, reduced-motion-säker; komponentkod prototypad/låst i chatt 2026-06-13) wire:as till verkligt laddnings-läge + spinner-vs-skeleton-doktrin dokumenteras + visual-verifieras.** Föregående: **PRODUKTNAMNBYTE JOBBPILOT → JOBBLIGGAREN LEVERERAD 2026-06-13 (ADR 0069 D1/D2/D4/D5/D6) — atomisk rename-PR på quiet tree (D7). Steg 0: ADR-PR #62 mergad först (ADR 0069 Accepted i main). Namespace `JobbPilot.*`→`Jobbliggaren.*` (~836 C#-filer, `.sln`, 13 `.csproj`, projektmappar); DB-namn/Postgres-roller/container/volym + JWT Audience `jobbpilot-api`→`jobbliggaren-api` (INGEN EF-migration — DbContexts redan brand-neutrala); `web/jobbpilot-web`→`web/jobbliggaren-web` + package-name; UrlFormat-attribut + README/runbook-URL:er; `build.yml` (.sln-ref + per-assembly coverage-gate-ids `check Jobbliggaren.X` + frontend-paths) + codeql/dependabot; living spec-docs BUILD/CLAUDE/DESIGN via approval-token (skill-tokens `jobbpilot-design-*`/`jobbpilot-td-lifecycle` BEVARADE — `.claude/skills/`-katalogerna ej omdöpta), README, ADR-index (rad 0015/0033 omdöpta, 0069-bridge-raden behåller båda namnen), issue-templates, tools-README. **EXKLUDERAT (D3, medvetet):** AWS Terraform + `deploy-dev.yml` (separat teardown-PR ADR 0066/TD-104), historiska session-loggar + ADR 0001–0068 (daterade records, ADR 0069 = tolknings-brygga), AWS-resurs-intrasslade runbooks + steg-tracker-/tech-debt-historikrader (gamla AWS-identifierare får INTE bli falska refs) + `.claude/`-tooling. **Mekanik:** per-spårad-fil `git mv` (lås-robust mot körande IDE) + 3-skiftlägs-sed; `.sln` + 13 `.csproj` REKONSTRUERADE från pristine `git show HEAD:` efter att VS Code C# Dev Kit BuildHost korrumperat projektgrafen mid-rename (prunade projekt-entries + ProjectReferences); en kontroll-tecken-binär-klassad testfil (SearchQueryParserTests) som `git grep -I` tyst hoppade fångad via `git grep -a`. **Test-isolation (rename-collateral, senior-cto-advisor Path A):** `C2ReverseLookupMigrationTests` (whole-table migration-replay) + `SearchCriteriaJsonbBackcompatTests` (lämnar legacy-`Ssyk`-rader) rensar nu `saved_searches` per test (`IAsyncLifetime`) — renamet omsorterade xunit:s namn-baserade ordning och avslöjade en latent delad-`[Collection("Api")]`-fixture-koppling; `Api.IntegrationTests` 476/0. **Gates:** build 0/0, .NET-svit grön (Domain/Application/Architecture/Migrate/Worker.Integration + Api.Integration 476/0), FE build/lint/vitest grön, dotnet format ren. **Reviews:** code-reviewer (0 Block/1 Major living-docs-titlar→löst i denna docs-sync/1 Minor pre-existing) + dotnet-architect ✓ GODKÄND (0/0/0) + security-auditor ✓ PASS (0/0/0). **Klas-STOPP/operativt:** (a) GitHub-repo-rename `jobbpilot`→`jobbliggaren` (din operation; GitHub 301-redirectar), (b) logo-PR separat nästa steg (bara wordmark-text ingick här; kompass-mark-redesign kvar per ADR 0068-not), (c) lokal DB recreate till `jobbliggaren` + stack-omstart.** Föregående: **FAS E KLASS 2 FILTERPANEL SLUTFÖRD 2026-06-13 — anställningsform + omfattning fullt filtrerbara på /jobb (panel, chips, facet-counts, recent-replay, Rensa). Levererad i 3 SoC-PR:er (CTO-ratificerad split): PR-1 #61 (backend options-källa, frusen taxonomi-seed + ADR 0043-amendment), PR-2 #63 (FE-panel: Omfattning radio / Anställningsform checkbox-multi, ärliga 8), PR-3 #64 (facet-counts) — ALLA MERGADE. PR-4 (saved-search Klass 2-labels) DEFERRAD-MED-TRIGGER (CTO-triage 2026-06-13): saved-search-listan har ingen FE-konsument → labels vore Speculative Generality; absorberas i framtida saved-search-list-UI-leverans (Pending #3). Agent-flöde: dotnet-architect + senior-cto-advisor (BESLUT 1–5 + PR-4-triage) + test-writer + nextjs-ui-engineer + code-reviewer (alla PR ✓; PR-2 1 Major recent-replay in-block) + design-reviewer (✓, FAS-DEFERRAL-MANIFEST rendered). RENDERED-VERIFIERING PENDING KLAS (auth-gated /jobb, stack nere): (1) Omfattning Deltid-före-Heltid vs Platsbankens Heltid-först, (2) pill-namn "Filter", (3) "Vanlig anställning" 24k synlig, (4) dark/fokus/pilnav/NVDA. TD-kandidat: border-strong-indikatorkontrast 2,52:1 (radio+checkbox, tvärgående).** Föregående: **FAS E PR-3 LEVERERAD 2026-06-13 — Klass 2 facet-counts (FE-only). Per-option-tal ("Heltid (29 427)", "Vanlig anställning (24 059)") i `JobbKlass2Panel` via 2 nya `useFacetCounts`-hooks (EmploymentType/WorktimeExtent), gated på "Filter"-panelen öppen. `FACET_DIMENSIONS` +2; `FacetCountsFilterState`/`FacetCountsFilter`/route-handler/hook-params +2 (Klass 2 ingår i facett-filtret → Ort/Yrke-facetterna reflekterar nu också anställningsform/omfattning, backend exkluderar egen dim). "Alla"-radion bär inget tal (SPOT = totalCount). `.jp-radioitem__count` co-selektor (= `.jp-checkitem__count`, noll nya tokens). Speglar E2c-kontraktet exakt (debounce/Abort/null-degradering/filterKey). Gates: tsc rent, vitest 38 (panel+hero, inkl. 4 nya count-tester), build grön. Reviews: code-reviewer ✓ 0/0/0 + design-reviewer ✓ 0/0/1 (moot — E2c renderar också "(0)"). FAS-DEFERRAL-MANIFEST rendered. Kvar: PR-4 saved-search-labels.** Föregående: **FAS E PR-2 LEVERERAD 2026-06-13 — FE Klass 2 filterpanel på /jobb. Ny `JobbKlass2Panel` (tredje "Filter"-pill): Omfattning = radio (Alla/Heltid/Deltid), Anställningsform = checkbox-multi (ärliga 8 råa JobTech-labels, ingen kurering — Klas-val). Full plumbing: `JobbUrlState`/`buildJobbHref`/`ListJobAdsQuery`/`buildQuery`/`chip-models` (DimensionAxis +2 + resolver) + toolbar-chips (Clock/FileText) + page.tsx-parsning + no-JS hidden inputs + "Rensa alla filter". Recent-search-replay bär nu Klass 2 (Major in-block: backend-DTO bar redan `employmentTypeList`/`worktimeExtentList` sedan B2 — FE konsumerade dem inte → tyst filter-drop; fixat + replay-test). `sameUrlState` komplett (Minor). Gates: tsc rent, vitest 128+ gröna, pnpm build grön, eslint rent. Reviews: nextjs-ui-engineer (bygge) + code-reviewer (1 Major + 1 Minor → båda in-block-fixade) + design-reviewer ✓ Approved (0/0/2 tvärgående: border-strong-kontrast = pre-existing checkbox-baseline → TD-kandidat; worktime-ordning Deltid/Heltid = rendered-review). FAS-DEFERRAL-MANIFEST: rendered-verifiering (dark/fokus/pilnav/NVDA) pending Klas lokalt (/jobb auth-gated, stack nere). Rendered-flaggor till Klas: (1) Omfattning Deltid-före-Heltid vs Platsbankens Heltid-först, (2) pill-namn "Filter", (3) "Vanlig anställning" 24k-bucket synlig. Kvar: PR-3 facet-wiring, PR-4 saved-search-labels.** Föregående: **FAS E PR-1 LEVERERAD 2026-06-13 — Klass 2 options-källa i taxonomi-ACL:n (anställningsform + omfattning som platta, föräldralösa dimensioner). Frusen embedded `klass2-taxonomy.json` (CTO BESLUT 1 Variant B — INTE generate.mjs), `TaxonomyConceptKind` +2, seeder (komposit-version + platta rader), `TaxonomyTreeDto` + egen `TaxonomyOptionDto`, FE `taxonomy.ts`-schema (REQUIRED), ADR 0043-amendment 2026-06-13. Ingen migration (Kind=string, bara nya rader). Gates: unit 25 seeder + 4 handlers, Testcontainers-integration 16 (inkl. reverse-lookup resolvar Klass 2 → aldrig "Okänd kod"), FE vitest 108, dotnet format + eslint rena, pnpm build grön. Reviews: dotnet-architect (advisor, variant-analys) + senior-cto-advisor (BESLUT 1–5) + code-reviewer ✓ Approved (0/0/0). Återstår i Fas E-kedjan: PR-2 (FE-panel: Omfattning radio / Anställningsform checkbox + Platsbanken-kurering), PR-3 (facet-wiring), PR-4 (saved-search-labels). BESLUT 5 löst via Platsbanken-bild (Omfattning=radio Alla/Heltid/Deltid, Anställningsform=checkbox-multi); kurering 8 rå vs 5 Platsbanken-etiketter = PR-2 rendered-review.** Föregående: **FAS B2 QUERY-WIRING LEVERERAD 2026-06-12 — anställningsform (`employment_type`) + omfattning (`worktime_extent`) sökbara end-to-end (`?employmentType=`/`?worktimeExtent=`). Sista datakedjan före Fas E Filter-panel klar. Variant A (Klas-override av CTO-rek B+TD): full ripple inkl. RecentJobSearch + migration i SAMMA PR. Alla gates gröna (Domain 471 / Application 789 / Api.Integration 473 Testcontainers / Worker 70 / Architecture 78 / FE 839). Reviews: code-reviewer ✓(0/0/1) + dotnet-architect(0/0/2) + security-auditor ✓ APPROVED(0/0/0). PR öppen, automerge-label.** Föregående: B2-DISCOVERY + re-ingest-beslut (#58/#59 mergade — data ~79% populerad). CC-STRUKTUR COLD REVIEW (#55/#56/#57). PLATSBANKEN SÖK-PARITET FAS E2j MERGAD #54. B2-discovery + äldre faser arkiverade i `docs/current-work-archive.md`.

**Levererat denna session 2026-06-12 (Fas B2 query-wiring — Klass 2 sökbar):**

- **Scope (Klas-override):** senior-cto-advisor dömde Variant B-i-PR + genuin TD (CCP/SRP). Klas valde **Variant A** (AskUserQuestion) — hela RecentJobSearch + migration-ripplet i samma PR. CTO erbjöd själv override-noten. Memory `project_b2_variant_a_full_ripple`.
- **Klass 2 = ortogonala IN-dims** (ej geo-union). Full ripple: `SearchCriteria`-VO (2 fält + invarianter), `FilterHashCalculator` (canonical-JSON emp/wt mellan region/sortBy — additiv format-bump), `RecentJobSearch` (+2 `text[]`-kolumner via migration `20260612120000_B2RecentJobSearchKlass2Columns`), `ICapturesRecentSearch` + capture-guard, `JobAdFilterCriteria` + alla 5 konsumenter, `FacetDimension` (enum-append + ShadowColumn + ExcludeDimension egen-lista), `SearchCriteriaConverters` (jsonb missing-key→tom, additivt = ingen saved_searches-migration), Create/Update SavedSearch + validatorer, endpoint-bindning GET / + /facet-counts. DTOs bär råa Klass 2-listor UTAN labels (Fas E-concern).
- **FE:** `saved-searches.ts` zod-drift städad i samma svep (ssyk→occupationGroup/municipality + Klass 2, MAX_CONCEPT_IDS 10→400) + test. Inga Filter-panel-ändringar (Fas E).
- **Mandatory agents:** test-writer FÖRST (RED→GREEN), db-migration-writer (recent_job_searches-migration), code-reviewer + dotnet-architect + security-auditor (alla gröna, 0 Blocker/0 Major; 3 Minor in-block: migration-staging + 2 doc-kommentarer).
- **Operativt:** migration applicerad på dev-DB (DDL + `__EFMigrationsHistory`-rad via container — EF-tooling-auth strulade); stacken omstartad (Api 5049 + Worker uppe — stoppades för full build pga exe-lås). ADR 0067 impl-notat 2026-06-12 skrivet.

**Levererat denna session 2026-06-12 (Fas B2-discovery + re-ingest-beslut — ingen kod):**

- **Discovery (inga ändringar, CLAUDE.md §9.4):** verifierat on-disk att B2:s data-lager är komplett och mergat — STORED-migration `20260608205054_F6P7JobAdKlass2SearchColumns` (employment_type + worktime_extent + partial-index), JobTechHit-POCO (`JobTechEmploymentType`/`JobTechWorkingHoursType`), sanitizer-allowlist, JobAdConfiguration shadow-mapping (`EmploymentTypeConceptId`/`WorktimeExtentConceptId`), `BackfillJobAdKlass2Job` + `POST /backfill-klass2`, generated-columns-tester. Promptens premiss ("kolumnerna saknas") motbevisad. Full rapport: `docs/research/2026-06-12-fas-b2-state-discovery.md`.
- **Faktisk lucka = query-wiringen:** `JobAdFilterCriteria`, `ApplyCriteria`-grenar, `ListJobAdsQuery`+validator, `?employmentType=`/`?worktimeExtent=`-bindning, `SearchCriteria`-VO-fält, `FacetDimension`-värden — alla medvetet utelämnade per ADR 0067 (Beslut 6 + C1/C2/D1/D2-notat: "följer query-wiring post re-ingest" / "i samma PR som re-ingestens data").
- **CTO-triage (decision-maker, §9.6) — sekvens-/fas-fråga mot Accepted-ADR-mekanik:** (1) re-ingest FÖRST, wira efteråt mot sann data — bygg INTE wiringen nu; (2) "FE-gated + Testcontainers-syntetisk" är otillräcklig falsk-klar-uppfyllelse — VO-ripplet till SavedSearch riskerar tyst-noll-bevakningar (ADR rad 41/115); (3) ingen legitim liten batch (CCP, Martin kap. 13); (4) Klas-GO krävs (prompt skriven under fel premiss). **Klas-GO: "Re-ingest först (CTO-rek)".**
- **Operativt utfört (Klas-GO):** Api + Worker startade lokalt; Admin-rollen tilldelad `klasolsson81@gmail.com` direkt i `identity."AspNetUserRoles"` (lokal dev-grant, reversibel, INGEN email-bootstrap — Klas-val); backfill triggad (`jobId 1587`, Succeeded ~4,5 min via per-ID-refetch av NULL-rader).
- **Empiriskt fynd (reviderar ADR-kommentarens "100% NULL"):** Klass 2 redan **~79% populerad bland Active** (34041/43340 emp, 31150 wt) — daglig snapshot-cron har re-ingestat med nya POCO:n sedan 2026-06-08. De ~21% NULL = purgad `raw_payload` (ADR 0032 retention) → **paritet med alla STORED-taxonomi-kolumner** (ssyk/region/occupation_group), ingen B2-regression. Siffrorna fluktuerar live (snapshot + purge kör). **Re-ingest-villkoret uppfyllt → query-wiring-PR kan byggas mot sann data direkt.** Detalj: `docs/research/2026-06-12-fas-b2-state-discovery.md` (Empiriskt fynd-sektionen).

**Levererat denna session 2026-06-12 (CC-struktur cold review — token-effektivisering):**

- **Cold review genomförd** (webb-verifierad mot code.claude.com 2026-06-12): VS Code-extensionen behålls (officiellt rekommenderad, samma tokenkostnad som CLI); Froject avvisad (GTM-målgrupp, `C:\DOTNET-UTB\froject-setup` raderad per Klas-GO); svenska→engelska för nya docs/commits (UI-copy + Klas-dialog förblir svenska).
- **Batch A (PR #55, automerge):** ADR 0002 Amendment 2026-06-12 — tier-alias (`opus`/`sonnet`/`haiku`) i agent-frontmatter ersätter explicita IDs (pinned ID kvar i runtime-config). Alla 13 agenter fick `model:`-fält (0/13 hade det on-disk): 8× opus, 3× sonnet, 2× haiku (test-runner + docs-keeper — mekaniskt arbete, 1/5 Opus-pris). `.claude/README.md` synkad (11→13 agenter). Memory `project_agent_model_field_drift` stängd.
- **Batch B (denna PR):** `session-start-template.md` omskriven 12→4 sektioner på engelska (~2–3k tokens sparas per session-start; duplicering av auto-laddad kontext struken). 5 största agent-filerna trimmade 16–21 KB → 5,5–6,6 KB styck (security-auditor, nextjs-ui-engineer, ai-prompt-engineer, code-reviewer, design-reviewer — roll/kriterier/veto/rapportformat bevarade; exempel och CLAUDE.md-duplicering strukna; agent-kroppen är subagentens systemprompt per dispatch). current-work.md trimmad (E2i–D2-block → arkivet).
- **Batch C (pågående):** TD-livscykel-skill + session-protokoll-runbook + CLAUDE.md-prune ≤3,5k tokens på engelska — **kräver Klas `approve-spec-edit.sh`**.

**Levererat session 2026-06-12 (Fas E2j — sök-commit-modell, MERGAD #54 `741727a`):**

- **Klas rendered-feedback på E2i live-spegel (modell-session, EJ mekanik):** tre kopplade problem — (1) recent-search "sparas inte som väntat" (**empiriskt bekräftat i dev-DB:** `recent_job_searches` full vid cap=20 för en seeker, fylld av live-`router.replace`-mellanstegsspam som evictade äkta committade sökningar — F3.3 "acceptera+observera" från E2h blev en defekt), (2) native `::-webkit-search-cancel-button` rensade texten men committade ingen delta → filtren överlevde, (3) djup-fråga: behövs Sök-knapp + × när resultat visas live?
- **architect + CTO INLINE (`docs/reviews/2026-06-12-sok-paritet-e2j-architect.md` + `-cto.md`):** capture-på-commit kräver explicit FE→BE-signal (backend kan inte skilja `router.replace` från `router.push`). Variant B (commit-flagga på befintlig list-query) är **materiellt skild** från ADR 0060 Beslut 3:s avvisade Variant B (separat command) — de fyra avvisnings-grunderna prövade verbatim, ingen träffar. → **ADR 0060 amendment 2026-06-12** (preciserar Beslut 3, ej ny ADR).
- **Klas-produktval (AskUserQuestion 2026-06-12, alla = CTO-rek):** (1) capture-trigger **B** (commit-flagga) + amendment-GO · (2) **behåll** Sök-knappen · (3) ×-semantik **(ii)** rensa text + de filter texten claimat, popover-val kvar · (4) toolbar-handlingar **bär commit=1**.
- **Backend (commit-guard):** `ICapturesRecentSearch.Commit` (markör-property); `RecentJobSearchCaptureBehavior` no-op:ar vid `Commit==false` (additiv till browse-/anonym-/invalid-guarderna); `ListJobAdsQuery.Commit=false`; endpoint binder `?commit=`. Test-writer FÖRST: behavior-tester (true/false/browse-additiv) + integration "live-sök utan commit fångar inte". 750 Application-unit gröna.
- **FE (`commit` strikt utanför `JobbUrlState`/`sameUrlState`/`buildJobbHref` — CTO väg 2):** `withCommitFlag`/`COMMIT_PARAM` (search-params.ts); commit-punkter (`onSubmitText` ALLTID, `onSelectSuggestion`, ny `onClear` = ×-semantik ii via `applyClaimsDelta(EMPTY_CLAIMS)`, toolbar) bär `?commit=1`; live-`onFieldChange` aldrig. **Render-sentinel skip-guard** (`else if sameUrlState(base, lastCommitted)`) skyddar own-roundtrip-detektorn + strip-efter-mount från falsk text-resync. Native × suppress:ad (CSS) + kontrollerad `.jp-hero__clearbtn`. No-JS statiskt hidden `commit=1`. Ny `StripCommitParam`-ö strippar `?commit=1` efter mount. **Popover-filter-klick bär medvetet INTE commit=1** (inkrementell komposition = live; CTO:s commit-punkt-lista exkluderade popover; data-minimerings-konservativt — bekräftat av security + code-review).
- **Reviews (alla Approved, inga blockers):** security-auditor **APPROVED 0/0/0** (PII-insamlingsväg-ändring verifierad (a)–(e); data-minimering Art. 5(1)(c) STÄRKT) · code-reviewer **Approved 0 Block/0 Major/3 Minor** (skip-guard-ordning verifierad; soft-hyphen-Minor fixad in-block) · design-reviewer **Approved 0 Blocker/0 Major/2 Minor** (WCAG 2.4.7 ×-fokusring-mönstret korrekt återanvänt; FAS-DEFERRAL-MANIFEST för rendered på auth-gated /jobb).
- **Gates:** tsc/eslint rena, **837 vitest** (+7 från 830-baslinjen), **pnpm build grön** (RSC — `/jobb` Dynamic), 750 Application-unit. ADR 0060 amendment + ADR 0067 impl-notat (E2j) skrivna.
- **CI-fångad fix (`47d60f1`):** `?commit=1` → `?commit=true` — ASP.NET `bool`-binding tar inte "1" (hade 400:at list-queryn). 8/8 RecentSearches-integrationstester verifierade lokalt mot Testcontainers efter fix. Lärdom: kör integration lokalt, inte bara unit/vitest.
- **Pending Klas:** rendered-granskning på auth-gated /jobb (×-knapp i båda teman, fokus-retur efter clear, commit-flödet Sök→Senaste sökningar live). Popover-commit-scoping (medvetet live) kan justeras på Klas-signal.

**Commit-kedja (squash-merge-SHA på main, sök-paritet-bågen):**

| SHA | PR | Beskrivning |
|---|---|---|
| `01a6039` | #29 | Fas A — ADR 0067 + ADR 0043-amendment (design) |
| `154fb07` | #30 | Fas B1 — Klass 1 data-layer (kommun/yrkesgrupp STORED) |
| `1fd9600` | #31 | Fas B2 — Klass 2 data-layer (anställningsform/omfattning + re-ingest-trigger) |
| `bc54a84` | #32 | Fas C1 — query/filter-layer + yrke-nivåbyte |
| `481e1f0` | #33 | Fas C2 — VO-expansion + reverse-lookup-migration + jsonb-bakåtkompat |
| `cefa60f` | #34 | chore/editor-baseline — .editorconfig + .vscode + docs-drift-fix |
| `e06c678` | #35 | docs(spec) — CLAUDE.md §1.6-rad current-work-archive |
| `06b7840` | #36 | docs(design) — handoff-bundles + agent-roster-CTO-rapport |
| `ed959c0` | #37 | Fas D1 — facet-counts + utökad typeahead-suggest |
| `13eb0af` | #38 | Fas D2 — ISearchQueryParser residual-fritext |
| `86b61ae` | #39 | Fas E1b — typeahead-suggest FE-kontrakt SuggestionDto[] |
| `f860ddf` | #40 | Fas E1a — /jobb-hero varm papperston-canvas (Papperskontoret) |
| `2922a25` | #41 | Fas E2a — yrke-nivå-skifte → yrkesgrupp (ssyk-level-4) |
| `7140c6b` | #42 | Fas G1 — grön accent-identitet + F4-banner (ADR 0068) |
| `74a25a9` | #43 | Fas G2 — banner-konsekvens (Sök jobb, 1136-alignment, F4-platta-rollout) |
| `08abb7b` | #44 | Fas G3 — konsekvensfixar (Sök jobb top-left, vit pagehero-CTA, a:hover-rotfix) |
| `c43a9d8` | #45 | Fas G4 — landing-redesign (produkt-forward ljus hero, login → topbar) |
| `cb42575` | #46 | Fas E2b — Län→Kommun-kaskad + geo-union region∪kommun |
| `0a4f48d` | #47 | Fas E2e — Rensa-röda-textlänkar + sorterings-labels |
| `65c3c8f` | #48 | Fas E2c — facet-counts-endpoint + NBomber-gate + FE live-counts |
| `e4071fe` | #49 | Fas E2f — tom kaskad-kolumn + markerade kommun-rader + de-gröna rubriker |
| `c3a8b57` | #50 | Fas E2g — hero-ö-state-synk (useOptimistic) + recent-search-labels |
| `1061bc2` | #51 | Fas E2d — typeahead-chip-komponist |
| `5f4e1cc` | #52 | Fas E2h — chips-i-sökfältet (UI ersatt av E2i) |
| `438a770` | #53 | Fas E2i — spegel-sökfält |
| `741727a` | #54 | Fas E2j — sök-commit-modell |

---

## Pending operativt för Klas

1. **Post-merge-granskning E2b (ADR 0065 automerge):** PR-diffen + Vercel-rendering av Ort-pickern (Län→Kommun, "Hela länet"-label) — design-reviewer godkände mot kod/diff; rendered-verifiering var pending live-deploy per runbook (auth-gated /jobb). Notera ram-utvidgningen: ~10 backend-rader geo-union (CTO-dom — Platsbanken-semantik var union, inte AND; full motivering i `docs/reviews/2026-06-11-sok-paritet-e2b-cto.md`).
2. **KLAS-STOPP — chip/residual-kombinationssemantik (ADR 0067 Beslut 5):** krävs INNAN E2d wirar chip+residual. Bekräfta `(dim-predikat) AND (FTS ∨ title-LIKE ∨ synonym)`. Natt-promptens bekräftelse-rad lämnades tom → E2d HALT.
3. **SavedSearch-list-FE-yta (oschemalagd) — deferral-register:** Saved-search-listan (`ListSavedSearches`) har INGEN FE-konsument idag — `/sokningar` pivoterade till RecentSearch (ADR 0039-amendment 2026-05-20), ingen `getSavedSearches`-API-klient finns, och `SavedSearchDto`:s `*Labels` renderas ingenstans. **När saved-search-list-UI faktiskt byggs levereras i SAMMA touch (CCP, CTO-triage 2026-06-13 `docs/reviews/2026-06-13-sok-paritet-e-klass2-pr4-triage-cto.md`):** (a) `getSavedSearches`-klient + `saved-searches.ts`-schema-konsumtion (zod redan B2-städad till occupationGroup/municipality/region + Klass 2-listor), (b) rendering av **alla fem** dimensioners `*Labels` (occupationGroup/municipality/region **+ employmentType/worktimeExtent**), (c) backend `EmploymentTypeLabels`/`WorktimeExtentLabels` i `ListSavedSearchesQueryHandler` via kind-agnostisk `ResolveLabelsAsync` (trivial — Klass 2 i taxonomi-ACL:n sedan PR-1 #61). **Ex-PR-4 (Fas E Klass 2) absorberas hit** — byggdes EJ nu (Speculative Generality mot odöd label-yta, §5/YAGNI). EJ numrerad TD.
4. **Logo-översyn (separat, Klas-ägd):** guld `#FFCD00` vs handoffens `#E8C77B` + og/twitter-wordmark — tas när du vill.
5. **Re-ingest Klass 2 — UTFÖRD 2026-06-12.** Backfill körd (`jobId 1587` Succeeded) + organisk snapshot-cron sedan 2026-06-08. Empiriskt: ~79% populerad bland Active (34041/43340 emp). De ~21% NULL = purgad `raw_payload` (paritet med alla taxonomi-kolumner, ej B2-bugg). **Nästa = query-wiring-PR** (JobAdFilterCriteria + ApplyCriteria + ListJobAdsQuery/validator + `?employmentType=`/`?worktimeExtent=` + SearchCriteria-VO-expansion + FacetDimension-append) mot sann data, test-writer FÖRST, Testcontainers-integration. Detalj: `docs/research/2026-06-12-fas-b2-state-discovery.md` (Empiriskt fynd-sektionen).
6. **CLAUDE.md §11.3-drift** (`make dev`/`pnpm dev:up` finns ej) — skapa-vs-stryk-beslut vid nästa spec-touch (kvarstår).
7. **"Obestämd ort/Utomlands"** — deferrad med payload-verifierings-trigger (ADR 0067 impl-notat E2b); explicit rest mot TD-100-stängningen.
8. **Rendered-granskning E2j** (×-knapp båda teman, fokus-retur, commit-flödet) — se E2j-blocket.

---

## Historik

All tidigare session-historik (Fas E2i, E2h, E2d, E2c, E2e, E2b, E2a, E1a, E1b, D2, D1, editor-baseline, Fas C2 och bakåt): **`docs/current-work-archive.md`** (omvänd kronologi) + per-session-loggar i **`docs/sessions/`**.
