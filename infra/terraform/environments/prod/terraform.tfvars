notification_emails = [
  "klasolsson81@gmail.com",
]

# monthly_budget_usd        = 50       # default — vill ha tidig varning under dev/lean-fas.
#                                       Höjs vid Fas 4 (AI-features driver Bedrock-cost) eller MVP-närmande.
# cloudtrail_retention_days = 90       # default
# eu_inference_profile_ids  = [...]    # default, uppdatera efter Bedrock-approval
